# Progress Tracker — Locumii

Update this file after every meaningful implementation change.

-----

## Current Phase

- **Phase 0 — Foundation & Specification**

## Current Goal

- Complete all planning and specification documents before writing a single line of application code.

-----

## Completed

- [x] **Market research** — Validated the ShiftKey model, identified the Nigerian locum staffing gap, confirmed ProLocums Nigeria as early-stage competition with low tech sophistication.
- [x] **Product decision** — Chose a two-sided shift marketplace for Nigerian healthcare professionals (doctors, nurses, pharmacists, medical lab scientists) and facilities (clinics, hospitals, diagnostic labs).
- [x] **`project-overview.md`** — One-paragraph summary, 6 goals, full professional and facility user flows, features by category, in-scope, out-of-scope, and 8-point success criteria.
- [x] **`architecture.md`** — Full stack table (React + Supabase + Paystack + Termii), system boundaries with folder ownership rules, storage model (PostgreSQL / Supabase Storage / Zustand), auth and RLS model, Edge Function definitions, and 7 invariants.
- [x] **`ai-workflow-rules.md`** — Spec-driven agent rules covering scoping, task splitting, ambiguity handling, protected files, documentation sync, and a full verification checklist with invariant gates.
- [x] **`ui-context.md`** — Complete color token system (brand, neutral, semantic status, shift status badges), typography scale (DM Serif Display + DM Sans + JetBrains Mono), border radius scale, spacing scale, shadow scale, component color assignments (buttons, inputs, nav), verified badge spec, and WCAG AA accessibility notes.
- [x] **`locumii-landing.html`** — Visual landing page proof-of-concept rendering the full UI context: teal/amber palette, shift cards with status badges, stats bar, for-professionals and for-facilities panels, notification stack, and footer.
- [x] **Project scaffold** — Vite + React (JavaScript, no TypeScript per `code-standards.md`) at the repo root alongside `Context/`. Tailwind CSS v4 via `@tailwindcss/vite`; `@/*` import alias in `vite.config.js` + `jsconfig.json`.
- [x] **Editor navbar (`Feature-specs/02-editor.md`)** — `src/components/editor/editor-navbar.jsx`: fixed-height (`h-14`) top navbar with left/center/right sections; left holds a ghost icon `Button` that toggles the sidebar, swapping `PanelLeftOpen`/`PanelLeftClose` (lucide) on `isSidebarOpen`; center and right sections present but empty; pure-neutral dark background (`bg-neutral-950`) with a subtle bottom border (`border-neutral-800`). Dumb component — takes `isSidebarOpen`/`onToggleSidebar` props, holds no state. **Done-checks passed:** `vite build` succeeds, `eslint` clean, esbuild transform parses OK.
- [x] **Editor layout (`Feature-specs/02-editor.md`)** — `src/components/editor/editor-layout.jsx`: full-height (`h-screen`) flex column that frames every editor screen. Owns the ephemeral `isSidebarOpen` state (local `useState`, not Zustand) and the named `handleToggleSidebar` handler, passing both into `EditorNavbar`. Below the navbar: a collapsible `aside` sidebar shell (`w-64` ↔ `w-0`, animated, right border, empty pending its spec) plus a scrollable light `main` content region rendering `children`. Mounted in `src/App.jsx`, replacing the Vite scaffold so the chrome actually renders. **Done-checks passed:** `vite build` succeeds (editor modules bundled, JS ~226 kB), `eslint` clean on app/editor files.
- [x] **Design system & UI primitives (`Feature-specs/01-design-systems.md`)** — `shadcn/ui` installed & configured for JSX (`components.json` `"tsx": false`, neutral base color, Geist font / Nova preset, Lucide icons). Components added to `src/components/ui/`: Button, Card, Dialog, Input, Tabs, Textarea, ScrollArea (left unmodified per spec). `lucide-react` installed. Reusable `cn()` helper at `src/lib/utils.js`. **Done-checks passed:** all 7 components bundle without import errors (esbuild through the `@` alias + node_modules), and `cn()` correctly handles conditionals, nested arrays/objects, and Tailwind conflict resolution.
- [x] **Database schema — all 10 tables (`architecture.md` Storage Model)** — Migrations `supabase/migrations/001`–`013` written and applied to the live project (`akfepyrsbtjyctatsxns`). `001` enum types; `002`–`011` the ten tables (`users`, `professional_profiles`, `facility_profiles`, `credentials`, `shifts`, `bids`, `shift_confirmations`, `transactions`, `ratings`, `notifications`), each with RLS enabled and explicit policies; `012` the private `credentials` Storage bucket + storage RLS (INV-06); `013` security/index hardening (advisor remediations). **All seven invariants enforced at the DB layer:** INV-02 (`bids_enforce_verified` trigger + RLS `with check`), INV-03 (`bids_enforce_no_overlap` overlap-prevention trigger), INV-04 (`transactions_enforce_immutable` trigger), INV-07 (`shifts_enforce_status_transition` state-machine trigger); INV-01/05/06 are policy/secret-handling rules not yet exercised by code. **Done-checks passed:** all 13 migrations applied without error; `get_advisors(security)` returns **zero** findings; 12 triggers verified attached; 9 enum types + 2 `private`-schema RLS helpers + private bucket confirmed via catalog query.
- [x] **Auth flow — all 5 steps (`architecture.md` Auth and Access Model).** End-to-end email/password auth on Supabase. (1) `supabase/migrations/014_handle_new_user.sql` — `SECURITY DEFINER` trigger that creates the `public.users` row and sets the `app_metadata.role` JWT claim on sign-up; `admin` self-registration rejected (verified live). (2) `src/store/authStore.js` — Zustand session slice (`userId`/`email`/`role`/`isVerified`/`session`/`isInitialized`). (3) `src/hooks/useAuth.js` — `useAuthListener()` (session bootstrap + `onAuthStateChange` sync, pulls `is_verified` from the profile) and `useAuth()` (`register`/`login`/`logout`/`resetPassword`/`updatePassword` + state). (4) `src/pages/auth/{Register,Login,ForgotPassword,Reset}.jsx` — shadcn-based forms with client validation. (5) `src/App.jsx` — React Router with `ProtectedRoute` (role-aware) + `PublicOnlyRoute` guards, `useAuthListener` mounted at root, temporary authenticated home at `/` (`src/pages/Home.jsx`); `/auth/reset` left unguarded for the recovery session. **Done-checks passed:** `vite build` succeeds (185 modules); lint clean on all auth files; registration trigger verified on the live DB. **Depends on dashboard config:** "Confirm email" should be OFF (or SMTP configured) for sign-up to complete — see Next Up.
- [x] **Professional onboarding (both steps).** Two-step flow off the temporary Home. **Step 1 — profile:** `src/hooks/useProfile.js` (`useSaveProfessionalProfile` upserts `professional_profiles`; never writes the trigger-protected `is_verified`/`avg_rating`) + `src/pages/professional/Onboarding.jsx` (name, specialty, council reg number, years, FCT preferred locations, optional bio; client validation). **Step 2 — documents:** `src/hooks/useCredentials.js` (`useUploadCredential` uploads to the private `credentials` bucket at `{userId}/{docType}/{name}` then inserts a `pending` `credentials` row) + `src/pages/professional/DocumentUpload.jsx` (MDCN license / NYSC cert / gov ID; PDF/PNG/JPEG ≤10 MB validation mirroring the bucket). Routes `/professional/onboarding` → `/professional/documents` (both `allowedRoles=['professional']`); option lists in `src/constants/options.js`. **Decisions:** fixed FCT city list; bank details deferred to payments. **Done-checks passed:** `vite build` + lint clean; profile upsert payload verified against the live table. Storage upload + RLS path validated by construction (object path satisfies the bucket's `foldername[1] = auth.uid()` policy; insert sets `professional_id = auth.uid()`, `status = 'pending'`) — pending a browser round-trip test.
- [x] **Facility onboarding (profile only).** `src/hooks/useProfile.js` (`useSaveFacilityProfile` upserts `facility_profiles`; never writes the trigger-protected `is_verified`/`avg_rating`) + `src/pages/facility/Onboarding.jsx` (facility name, type, CAC number, address, FCT location, contact person; client validation; `state` fixed to `'FCT'` for the Abuja launch). No document upload — facilities are verified by a manual admin check. Wired at `/facility/onboarding` (`allowedRoles=['facility']`) with a "Complete your facility profile" link on the temporary Home. `FACILITY_TYPES` added to `src/constants/options.js`. **Done-checks passed:** `vite build` + lint clean; upsert payload verified against the live `facility_profiles` table (enum `facility_type`, `is_verified` defaults false), then cleaned up.
- [x] **First admin seeded.** `abdulhafizakinsola@gmail.com` created directly in the live DB (per `architecture.md` — admins are not self-registerable): `auth.users` row with a bcrypt password + confirmed email + `email` identity, `public.users` row `role='admin'`/`status='active'`, and `app_metadata.role='admin'` for RLS. Seeded with a temporary password (not committed) to be changed later. **Not** captured as a migration file (one-off, contains a credential). **Gotcha (fixed):** a hand-seeded `auth.users` row must set GoTrue's token columns (`confirmation_token`, `recovery_token`, `email_change_token_new`, `email_change`, `email_change_token_current`, `phone_change`, `phone_change_token`, `reauthentication_token`) to `''` — leaving them NULL makes GoTrue fail every login with `500: Database error querying schema` ("converting NULL to string is unsupported"). Future direct user seeds must include these.
- [x] **Admin credential review panel.** `src/hooks/useAdminCredentials.js` (`usePendingCredentials` — admin-only fetch of pending credentials with the owning professional embedded; `useReviewCredential` — `reviewCredential` approve/reject with `reviewed_by`/`reviewed_at`, `verifyProfessional` flips `is_verified`, `getDocumentUrl` mints a ≤15-min signed URL per INV-06) + `src/pages/admin/CredentialQueue.jsx` (`/admin/credentials`, `allowedRoles=['admin']`; groups pending docs by professional, View/Approve/Reject per doc + "Mark professional verified"). "Review credentials" link added to Home for admins. **Done-checks passed:** `vite build` + lint clean. **Browser-test pending:** RLS-gated admin reads/writes, signed-URL document view, and the embed query exercise only with the admin JWT (SQL bypasses RLS). **Deferred:** SMS-on-approval (needs `send-sms` Edge Function + Termii).
- [x] **Facility shift posting (no payment yet).** `src/utils/money.js` (`nairaToKobo`/`koboToNaira`/`formatNaira` — amounts stored as integer kobo) + `src/hooks/useShifts.js` (`useCreateShift` inserts an `open` shift owned by the facility; `paystack_reference` stays null) + `src/pages/facility/PostShift.jsx` (`/facility/post-shift`, `allowedRoles=['facility']`; role, start/end `datetime-local`, pay rate in ₦→kobo, FCT location, optional requirements; client validation incl. end>start and start in future). "Post a shift" link on the facility Home. **Decisions/notes:** Paystack upfront payment is deferred to its own unit (shift is created directly as `open`); posting is **not** gated on facility verification yet (no DB invariant / no facility-review panel). The `facility_profiles` FK means a facility must onboard first — surfaced as a friendly error otherwise. **Done-checks passed:** `vite build` + lint clean; shift insert payload verified against the live table (kobo `pay_rate_naira`, `status='open'` default, `end>start` check), then cleaned up.
- [x] **Professional shift feed — browse (step B1).** Read-only browse of open shifts. `src/utils/dateTime.js` (`formatShiftRange`/`formatDateTime`) + `src/hooks/useShifts.js` (`useOpenShifts({city,role,date})` — filtered, facility embedded, 20-row page; `useShift(id)`) + `src/components/shifts/ShiftCard.jsx` (dumb; ₦ in `font-mono` per standards) + `src/pages/professional/ShiftFeed.jsx` (`/professional/shifts`, city/role/date filters) + `src/pages/professional/ShiftDetail.jsx` (`/professional/shifts/:shiftId`). "Browse shifts" link on the professional Home. **Seed data:** a demo facility (`demo-facility@locumii.test`, "Demo Hospital (test)", id `6d100000-…-0001`) + two open shifts seeded so the feed has content — removable on request. **Done-checks passed:** `vite build` + lint clean. **Browser-test pending:** the feed/detail render with the admin-RLS-free open-shift read (works for any authenticated user). **Remaining (step B2):** bidding — `useBids` + bid button on the detail page, with the verified-gate (INV-02) and overlap prevention (INV-03) already enforced in the DB.
- [x] **Professional bidding (step B2).** `src/hooks/useBids.js` (`useShiftBid(shiftId)` — the professional's existing bid; `useSubmitBid` — inserts a pending bid) + `src/components/shifts/BidButton.jsx` (dumb; shows submit / verification-required / existing-bid-status / shift-closed) wired into `ShiftDetail`. UI gates bidding on `isVerified`; the DB enforces INV-02 (verified-only) and one-bid-per-shift. **Done-checks passed:** `vite build` + lint clean; **INV-02 verified live** — an unverified professional's bid is rejected by the `bids_enforce_verified` trigger ("An unverified professional cannot bid on a shift (INV-02)"). Incidentally confirmed the `professional_profiles` guard blocks `is_verified` changes without an admin JWT (so the admin verify action depends on the admin's `app_metadata.role` claim, present in-browser). The verified→bid happy path and INV-03 (overlap, enforced on *accept*) are browser/accept-flow tested.
- [x] **Facility bid management + `accept_bid` (the accept lifecycle).** `supabase/migrations/015_accept_bid.sql` — `accept_bid(p_bid_id)` `SECURITY DEFINER` RPC that atomically authorizes the caller owns the shift, accepts the bid (fires INV-03 overlap check), cancels the other pending bids, and moves the shift `open → filled` (INV-07). `016`/`017` tighten function grants so only `authenticated` can call it (anon/PUBLIC revoked; same applied to `handle_new_user`). Frontend: `useFacilityShifts` + `useShiftBids` + `useAcceptBid` (calls the RPC) + `src/pages/facility/MyShifts.jsx` (`/facility/shifts`) and `ManageBids.jsx` (`/facility/shifts/:shiftId/bids`, Accept per pending bid) + "My shifts" Home link. **Decision/deviation:** accept is a `SECURITY DEFINER` Postgres RPC, **not** an Edge Function — chosen for atomic multi-row work and to avoid exposing `service_role`; same security boundary (no client `UPDATE bids`). The architecture's separate webhook-driven `auto-cancel-bids` is folded into this RPC; SMS-on-decline still deferred. **Done-checks passed:** `vite build` + lint clean; verified live (non-owner blocked; accept → other bid cancelled → shift filled). **Advisors:** only two remain, both expected — `accept_bid` is intentionally `authenticated`-callable (authorizes internally via `auth.uid()`), and "leaked password protection" is a one-click Auth dashboard toggle to enable before launch.
- [x] **Shift completion flow (check-in + dual confirmation).** `supabase/migrations/018_shift_completion.sql` — `check_in_shift(p_shift_id)` RPC (accepted professional only; `filled → in_progress`) + `complete_shift_on_dual_confirm()` trigger on `shift_confirmations` (when both timestamps set, `in_progress → completed`, fires INV-07). `019` tightens grants (anon/PUBLIC revoked; `authenticated` keeps the RPC). Hooks `src/hooks/useShiftConfirmation.js` (`useShiftConfirmation`, `useCheckInShift`, `useConfirmCompletion` — picks the column by role; the guard trigger independently enforces each side writes only its own). UI: professional `ShiftDetail` shows **Check in** (filled) then **Confirm completion** (in_progress); facility `ManageBids` shows **Confirm completion** (in_progress); both show "completed". **Assumptions (spec was loose):** A1 check-in = professional action; A2 completion = dual confirm → trigger; A3 payment release deferred to Paystack; A4 buttons status-gated in the UI. **Done-checks passed:** `vite build` + lint clean; **full lifecycle verified live** (accept→filled, check-in→in_progress, single confirm→still in_progress, dual confirm→completed). **Advisors:** 3 expected (the `accept_bid` + `check_in_shift` intentional RPCs, plus the dashboard leaked-password toggle).

-----

## In Progress

- **Editor chrome (`Feature-specs/02-editor.md`)** — Navbar + layout shell built earlier; **no longer mounted at `/`** (step 5 of the auth flow replaced `App.jsx` with the router). The components remain in `src/components/editor/` for reuse once their place in the app is specified. The sidebar's *contents* are still unspecified.

-----

## Next Up

1. **`dev-setup.md`** — Local environment setup: Node version, Supabase CLI install, environment variables required, how to run migrations, how to start the dev server.
1. **Supabase Auth config (dashboard)** — Confirm email/password provider is enabled and SMTP (Resend) is wired. The DB schema and private `credentials` bucket are done; this is the remaining piece of the original "project initialisation" item.
1. **SMS-on-approval (deferred from credential review)** — Fire an SMS to the professional when their credentials are approved. Needs the `send-sms` Edge Function + Termii (see Open Questions). The review panel currently flips `is_verified` without notifying.
1. **Paystack payment at posting** — Its own unit (needs a Paystack account/keys). Collect the shift fee + 10% upfront via the inline popup, verify server-side in an Edge Function, then store `paystack_reference` / mark the shift payable. Currently shifts are created `open` with no payment.
1. **Edge Functions** — `auto-cancel-bids`, `send-shift-reminder`, `release-payment`, `send-sms` (Deno/TS). Several deferred items above depend on these. Needs Termii + Paystack keys.
1. **Payment release on completion** — *Check-in + dual confirmation are done (above).* Remaining: the `release-payment` step that pays out the professional (minus 10%) once a shift is `completed`. Depends on Paystack (Transfer API, server-side only — INV-01) and the professional's bank details.
1. **Ratings** — Post-completion rating flow for both parties, display on profiles.

-----

## Open Questions

- **App name confirmed?** “Locumii” is the working title used across all documents. Confirm before buying a domain or setting up Supabase project name.
- **Abuja-only launch geography** — All spec documents scope the MVP to FCT. Confirm this before building location filters (hardcoded city list vs. open text field).
- **Admin user creation** — ✅ Resolved. First admin is `abdulhafizakinsola@gmail.com`, seeded directly in the DB (see Completed). Future admins follow the same direct-seed approach.
- **Termii vs. alternative SMS gateway** — Termii is specified in `architecture.md`. Confirm account is created and API key is available before building any Edge Function that calls `send-sms`.
- **Professional types** — Four types are in scope: Medical Doctor, Nurse, Pharmacist, Medical Laboratory Scientist. Confirm whether NYSC-posted health workers are treated as a separate type or fall under one of these four.
- **Paystack escrow mechanism** — Paystack does not have a native escrow product. The escrow model described in the spec is implemented by collecting the full amount upfront (Paystack inline) and holding it in the platform’s Paystack balance until the `release-payment` Edge Function initiates a transfer. Confirm this approach is understood before building the payment flow.

-----

## Architecture Decisions

- **Frontend:** React (Vite) — single-page app, mobile browser first, no native app for MVP.
- **Backend:** Supabase (PostgreSQL + PostgREST + Auth + Storage + Edge Functions) — no custom Express/Node server.
- **Payments:** Paystack — inline popup for collection, Transfer API (server-side only) for disbursement. The Paystack secret key never touches the frontend.
- **SMS:** Termii — all SMS routed through a single `send-sms` Edge Function; no other function or frontend code calls Termii directly.
- **Escrow:** Implemented at the application layer — funds collected upfront via Paystack inline, held in platform Paystack balance, released via Edge Function after dual confirmation.
- **Credential verification:** Manual admin review for MVP — no OCR, no government API integration.
- **Overlap prevention:** Enforced by a PostgreSQL constraint function on the `bids` table, not by application code alone.
- **Immutable ledger:** `transactions` table rows with `status = released` are locked by a PostgreSQL trigger — no UPDATE is permitted, disputes create new rows.
- **Launch geography:** Abuja FCT only. Lagos is Phase 2.
- **Color system:** Deep teal primary (`#0B6E6E`), amber-gold accent (`#D4900A`), burgundy critical (`#8A1A1A` — not red). Fonts: DM Serif Display (headings), DM Sans (body), JetBrains Mono (Naira amounts only).

-----

## Session Notes

- All five foundation documents (`project-overview.md`, `architecture.md`, `ai-workflow-rules.md`, `ui-context.md`, `locumii-landing.html`) are complete and consistent with each other. No implementation code has been written yet.
- The landing page HTML (`locumii-landing.html`) is a design proof-of-concept only — it is not part of the application source tree. It should not be moved into `src/`.
- The next session should begin with `dev-setup.md` and Supabase project initialisation before any React code is written.
- All seven architecture invariants (INV-01 through INV-07) are defined in `architecture.md` and referenced in the `ai-workflow-rules.md` verification checklist. Any agent starting work must read both files before writing code.
- The Paystack escrow model is application-layer, not a native Paystack feature — this must be clearly communicated to any developer or agent picking up the payments task.
- **Implementation has begun.** The design-system spec (`Feature-specs/01-design-systems.md`) is complete. This came before `dev-setup.md`/Supabase init (the original "Next Up" order) at the user's explicit direction. Node.js LTS (v24) was installed on the dev machine to enable the npm/npx toolchain.
- **Resolved spec conflict:** the design-system spec requested `lib/utils.ts`, but `code-standards.md` forbids TypeScript. Per user decision, the `cn()` helper stays at `src/lib/utils.js` (JavaScript). The spec's `.ts` is treated as a typo for this JS-only project. If the spec text should be corrected, that is a human edit (spec files are not auto-edited to match code).
- shadcn uses the **Nova** preset (Geist + Lucide) and **neutral** base color, aligning with `ui-context.md`. The stock shadcn theme tokens in `src/index.css` are **not yet** mapped to the Locumii teal/amber brand tokens — wiring the full brand token system is a separate, not-yet-scoped task.
- **Database schema is live (migrations 001–013, applied via Supabase MCP).** Implementation notes / minor deviations from `architecture.md`, surfaced here rather than edited into the spec (per `ai-workflow-rules.md` §5/§6 — spec files are human-edited only):
  - **Money columns hold kobo, not Naira.** Per user decision, the `*_naira` column names from `architecture.md` are kept, but the stored values are integer **kobo** (per `code-standards.md`). Column comments document this. `money.js` must divide by 100 for display. If the spec should be corrected to say `_kobo`, that is a human edit.
  - **Two RLS helper functions live in a `private` schema** (`private.is_shift_facility`, `private.is_accepted_shift_professional`), not `public`. They are `SECURITY DEFINER` (needed to avoid RLS recursion on `bids`/`shifts`); moving them out of the API-exposed `public` schema clears the advisor's "SECURITY DEFINER callable via RPC" warning. `public.is_admin()` stays in `public` (it is `SECURITY INVOKER` and only returns whether the caller is an admin).
  - **Shifts read access was extended** so a professional can still read a shift they have bid on once it leaves `open` status (`shifts_select_own_bids` policy in `007`). `architecture.md`'s RLS table only lists "open rows readable by any authenticated user" — this is a necessary corollary, not a contradiction.
  - **The `users` registration insert has no client RLS policy by design.** Inserting a `users` row is deferred to the auth-flow unit and must run through a `SECURITY DEFINER` trigger on `auth.users`, so a user cannot self-assign `role = 'admin'`.
  - **Deferred (not blocking, MVP scale):** the performance advisor's `auth_rls_initplan` warnings (wrap `auth.uid()`/`is_admin()` in `(select …)` inside policies) and one `multiple_permissive_policies` note on `shifts` SELECT remain. These are scale optimisations on currently-empty tables; revisit before launch via a dedicated policy-optimisation migration. `unused_index` INFOs are expected (no traffic yet).
- **Lint vs. fetch-on-mount:** the `react-hooks/set-state-in-effect` rule (eslint-plugin-react-hooks v7) errors on the architecture's prescribed "hooks own data fetching" pattern (a `useEffect` that calls a fetch which `setState`s). `usePendingCredentials` uses a one-line `// eslint-disable-next-line react-hooks/set-state-in-effect` for the mount fetch. Future list hooks (`useShifts`, `useBids`, …) will need the same. If this proliferates, consider setting that rule to `warn`/`off` in `eslint.config.js` project-wide — a deliberate config decision for the user, not done unilaterally.