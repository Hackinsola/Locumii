# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Build tooling caches / local platform dirs
.vite
.vercel

# Environment variables — real keys live here; copy .env.example to .env.
# .env.example is intentionally tracked.
.env
.env.*
!.env.example

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Agent skills — third-party repos cloned by `npx skills add` (manifest in skills-lock.json)
.agents/

# Local Claude Code settings and skill symlinks (machine-specific)
.claude/settings.local.json
.claude/skills/
