# Progress Tracker — Locumii

Update this file after every meaningful implementation change.

-----

## Current Phase

- **Phase 0 — Foundation & Specification**

## Current Goal

- Complete all planning and specification documents before writing a single line of application code.

-----

## Completed

- [x] **Market research** — Validated the ShiftKey model, identified the Nigerian locum staffing gap, confirmed ProLocums Nigeria as early-stage competition with low tech sophistication.
- [x] **Product decision** — Chose a two-sided shift marketplace for Nigerian healthcare professionals (doctors, nurses, pharmacists, medical lab scientists) and facilities (clinics, hospitals, diagnostic labs).
- [x] **`project-overview.md`** — One-paragraph summary, 6 goals, full professional and facility user flows, features by category, in-scope, out-of-scope, and 8-point success criteria.
- [x] **`architecture.md`** — Full stack table (React + Supabase + Paystack + Termii), system boundaries with folder ownership rules, storage model (PostgreSQL / Supabase Storage / Zustand), auth and RLS model, Edge Function definitions, and 7 invariants.
- [x] **`ai-workflow-rules.md`** — Spec-driven agent rules covering scoping, task splitting, ambiguity handling, protected files, documentation sync, and a full verification checklist with invariant gates.
- [x] **`ui-context.md`** — Complete color token system (brand, neutral, semantic status, shift status badges), typography scale (DM Serif Display + DM Sans + JetBrains Mono), border radius scale, spacing scale, shadow scale, component color assignments (buttons, inputs, nav), verified badge spec, and WCAG AA accessibility notes.
- [x] **`locumii-landing.html`** — Visual landing page proof-of-concept rendering the full UI context: teal/amber palette, shift cards with status badges, stats bar, for-professionals and for-facilities panels, notification stack, and footer.
- [x] **Project scaffold** — Vite + React (JavaScript, no TypeScript per `code-standards.md`) at the repo root alongside `Context/`. Tailwind CSS v4 via `@tailwindcss/vite`; `@/*` import alias in `vite.config.js` + `jsconfig.json`.
- [x] **Editor navbar (`Feature-specs/02-editor.md`)** — `src/components/editor/editor-navbar.jsx`: fixed-height (`h-14`) top navbar with left/center/right sections; left holds a ghost icon `Button` that toggles the sidebar, swapping `PanelLeftOpen`/`PanelLeftClose` (lucide) on `isSidebarOpen`; center and right sections present but empty; pure-neutral dark background (`bg-neutral-950`) with a subtle bottom border (`border-neutral-800`). Dumb component — takes `isSidebarOpen`/`onToggleSidebar` props, holds no state. **Done-checks passed:** `vite build` succeeds, `eslint` clean, esbuild transform parses OK.
- [x] **Editor layout (`Feature-specs/02-editor.md`)** — `src/components/editor/editor-layout.jsx`: full-height (`h-screen`) flex column that frames every editor screen. Owns the ephemeral `isSidebarOpen` state (local `useState`, not Zustand) and the named `handleToggleSidebar` handler, passing both into `EditorNavbar`. Below the navbar: a collapsible `aside` sidebar shell (`w-64` ↔ `w-0`, animated, right border, empty pending its spec) plus a scrollable light `main` content region rendering `children`. Mounted in `src/App.jsx`, replacing the Vite scaffold so the chrome actually renders. **Done-checks passed:** `vite build` succeeds (editor modules bundled, JS ~226 kB), `eslint` clean on app/editor files.
- [x] **Design system & UI primitives (`Feature-specs/01-design-systems.md`)** — `shadcn/ui` installed & configured for JSX (`components.json` `"tsx": false`, neutral base color, Geist font / Nova preset, Lucide icons). Components added to `src/components/ui/`: Button, Card, Dialog, Input, Tabs, Textarea, ScrollArea (left unmodified per spec). `lucide-react` installed. Reusable `cn()` helper at `src/lib/utils.js`. **Done-checks passed:** all 7 components bundle without import errors (esbuild through the `@` alias + node_modules), and `cn()` correctly handles conditionals, nested arrays/objects, and Tailwind conflict resolution.
- [x] **Database schema — all 10 tables (`architecture.md` Storage Model)** — Migrations `supabase/migrations/001`–`013` written and applied to the live project (`akfepyrsbtjyctatsxns`). `001` enum types; `002`–`011` the ten tables (`users`, `professional_profiles`, `facility_profiles`, `credentials`, `shifts`, `bids`, `shift_confirmations`, `transactions`, `ratings`, `notifications`), each with RLS enabled and explicit policies; `012` the private `credentials` Storage bucket + storage RLS (INV-06); `013` security/index hardening (advisor remediations). **All seven invariants enforced at the DB layer:** INV-02 (`bids_enforce_verified` trigger + RLS `with check`), INV-03 (`bids_enforce_no_overlap` overlap-prevention trigger), INV-04 (`transactions_enforce_immutable` trigger), INV-07 (`shifts_enforce_status_transition` state-machine trigger); INV-01/05/06 are policy/secret-handling rules not yet exercised by code. **Done-checks passed:** all 13 migrations applied without error; `get_advisors(security)` returns **zero** findings; 12 triggers verified attached; 9 enum types + 2 `private`-schema RLS helpers + private bucket confirmed via catalog query.
- [x] **Auth flow — all 5 steps (`architecture.md` Auth and Access Model).** End-to-end email/password auth on Supabase. (1) `supabase/migrations/014_handle_new_user.sql` — `SECURITY DEFINER` trigger that creates the `public.users` row and sets the `app_metadata.role` JWT claim on sign-up; `admin` self-registration rejected (verified live). (2) `src/store/authStore.js` — Zustand session slice (`userId`/`email`/`role`/`isVerified`/`session`/`isInitialized`). (3) `src/hooks/useAuth.js` — `useAuthListener()` (session bootstrap + `onAuthStateChange` sync, pulls `is_verified` from the profile) and `useAuth()` (`register`/`login`/`logout`/`resetPassword`/`updatePassword` + state). (4) `src/pages/auth/{Register,Login,ForgotPassword,Reset}.jsx` — shadcn-based forms with client validation. (5) `src/App.jsx` — React Router with `ProtectedRoute` (role-aware) + `PublicOnlyRoute` guards, `useAuthListener` mounted at root, temporary authenticated home at `/` (`src/pages/Home.jsx`); `/auth/reset` left unguarded for the recovery session. **Done-checks passed:** `vite build` succeeds (185 modules); lint clean on all auth files; registration trigger verified on the live DB. **Depends on dashboard config:** "Confirm email" should be OFF (or SMTP configured) for sign-up to complete — see Next Up.
- [x] **Professional onboarding (both steps).** Two-step flow off the temporary Home. **Step 1 — profile:** `src/hooks/useProfile.js` (`useSaveProfessionalProfile` upserts `professional_profiles`; never writes the trigger-protected `is_verified`/`avg_rating`) + `src/pages/professional/Onboarding.jsx` (name, specialty, council reg number, years, FCT preferred locations, optional bio; client validation). **Step 2 — documents:** `src/hooks/useCredentials.js` (`useUploadCredential` uploads to the private `credentials` bucket at `{userId}/{docType}/{name}` then inserts a `pending` `credentials` row) + `src/pages/professional/DocumentUpload.jsx` (MDCN license / NYSC cert / gov ID; PDF/PNG/JPEG ≤10 MB validation mirroring the bucket). Routes `/professional/onboarding` → `/professional/documents` (both `allowedRoles=['professional']`); option lists in `src/constants/options.js`. **Decisions:** fixed FCT city list; bank details deferred to payments. **Done-checks passed:** `vite build` + lint clean; profile upsert payload verified against the live table. Storage upload + RLS path validated by construction (object path satisfies the bucket's `foldername[1] = auth.uid()` policy; insert sets `professional_id = auth.uid()`, `status = 'pending'`) — pending a browser round-trip test.
- [x] **Facility onboarding (profile only).** `src/hooks/useProfile.js` (`useSaveFacilityProfile` upserts `facility_profiles`; never writes the trigger-protected `is_verified`/`avg_rating`) + `src/pages/facility/Onboarding.jsx` (facility name, type, CAC number, address, FCT location, contact person; client validation; `state` fixed to `'FCT'` for the Abuja launch). No document upload — facilities are verified by a manual admin check. Wired at `/facility/onboarding` (`allowedRoles=['facility']`) with a "Complete your facility profile" link on the temporary Home. `FACILITY_TYPES` added to `src/constants/options.js`. **Done-checks passed:** `vite build` + lint clean; upsert payload verified against the live `facility_profiles` table (enum `facility_type`, `is_verified` defaults false), then cleaned up.
- [x] **First admin seeded.** `abdulhafizakinsola@gmail.com` created directly in the live DB (per `architecture.md` — admins are not self-registerable): `auth.users` row with a bcrypt password + confirmed email + `email` identity, `public.users` row `role='admin'`/`status='active'`, and `app_metadata.role='admin'` for RLS. Seeded with a temporary password (not committed) to be changed later. **Not** captured as a migration file (one-off, contains a credential). **Gotcha (fixed):** a hand-seeded `auth.users` row must set GoTrue's token columns (`confirmation_token`, `recovery_token`, `email_change_token_new`, `email_change`, `email_change_token_current`, `phone_change`, `phone_change_token`, `reauthentication_token`) to `''` — leaving them NULL makes GoTrue fail every login with `500: Database error querying schema` ("converting NULL to string is unsupported"). Future direct user seeds must include these.
- [x] **Admin credential review panel.** `src/hooks/useAdminCredentials.js` (`usePendingCredentials` — admin-only fetch of pending credentials with the owning professional embedded; `useReviewCredential` — `reviewCredential` approve/reject with `reviewed_by`/`reviewed_at`, `verifyProfessional` flips `is_verified`, `getDocumentUrl` mints a ≤15-min signed URL per INV-06) + `src/pages/admin/CredentialQueue.jsx` (`/admin/credentials`, `allowedRoles=['admin']`; groups pending docs by professional, View/Approve/Reject per doc + "Mark professional verified"). "Review credentials" link added to Home for admins. **Done-checks passed:** `vite build` + lint clean. **Browser-test pending:** RLS-gated admin reads/writes, signed-URL document view, and the embed query exercise only with the admin JWT (SQL bypasses RLS). **Deferred:** SMS-on-approval (needs `send-sms` Edge Function + Termii).
- [x] **Facility shift posting (no payment yet).** `src/utils/money.js` (`nairaToKobo`/`koboToNaira`/`formatNaira` — amounts stored as integer kobo) + `src/hooks/useShifts.js` (`useCreateShift` inserts an `open` shift owned by the facility; `paystack_reference` stays null) + `src/pages/facility/PostShift.jsx` (`/facility/post-shift`, `allowedRoles=['facility']`; role, start/end `datetime-local`, pay rate in ₦→kobo, FCT location, optional requirements; client validation incl. end>start and start in future). "Post a shift" link on the facility Home. **Decisions/notes:** Paystack upfront payment is deferred to its own unit (shift is created directly as `open`); posting is **not** gated on facility verification yet (no DB invariant / no facility-review panel). The `facility_profiles` FK means a facility must onboard first — surfaced as a friendly error otherwise. **Done-checks passed:** `vite build` + lint clean; shift insert payload verified against the live table (kobo `pay_rate_naira`, `status='open'` default, `end>start` check), then cleaned up.
- [x] **Professional shift feed — browse (step B1).** Read-only browse of open shifts. `src/utils/dateTime.js` (`formatShiftRange`/`formatDateTime`) + `src/hooks/useShifts.js` (`useOpenShifts({city,role,date})` — filtered, facility embedded, 20-row page; `useShift(id)`) + `src/components/shifts/ShiftCard.jsx` (dumb; ₦ in `font-mono` per standards) + `src/pages/professional/ShiftFeed.jsx` (`/professional/shifts`, city/role/date filters) + `src/pages/professional/ShiftDetail.jsx` (`/professional/shifts/:shiftId`). "Browse shifts" link on the professional Home. **Seed data:** a demo facility (`demo-facility@locumii.test`, "Demo Hospital (test)", id `6d100000-…-0001`) + two open shifts seeded so the feed has content — removable on request. **Done-checks passed:** `vite build` + lint clean. **Browser-test pending:** the feed/detail render with the admin-RLS-free open-shift read (works for any authenticated user). **Remaining (step B2):** bidding — `useBids` + bid button on the detail page, with the verified-gate (INV-02) and overlap prevention (INV-03) already enforced in the DB.
- [x] **Professional bidding (step B2).** `src/hooks/useBids.js` (`useShiftBid(shiftId)` — the professional's existing bid; `useSubmitBid` — inserts a pending bid) + `src/components/shifts/BidButton.jsx` (dumb; shows submit / verification-required / existing-bid-status / shift-closed) wired into `ShiftDetail`. UI gates bidding on `isVerified`; the DB enforces INV-02 (verified-only) and one-bid-per-shift. **Done-checks passed:** `vite build` + lint clean; **INV-02 verified live** — an unverified professional's bid is rejected by the `bids_enforce_verified` trigger ("An unverified professional cannot bid on a shift (INV-02)"). Incidentally confirmed the `professional_profiles` guard blocks `is_verified` changes without an admin JWT (so the admin verify action depends on the admin's `app_metadata.role` claim, present in-browser). The verified→bid happy path and INV-03 (overlap, enforced on *accept*) are browser/accept-flow tested.
- [x] **Facility bid management + `accept_bid` (the accept lifecycle).** `supabase/migrations/015_accept_bid.sql` — `accept_bid(p_bid_id)` `SECURITY DEFINER` RPC that atomically authorizes the caller owns the shift, accepts the bid (fires INV-03 overlap check), cancels the other pending bids, and moves the shift `open → filled` (INV-07). `016`/`017` tighten function grants so only `authenticated` can call it (anon/PUBLIC revoked; same applied to `handle_new_user`). Frontend: `useFacilityShifts` + `useShiftBids` + `useAcceptBid` (calls the RPC) + `src/pages/facility/MyShifts.jsx` (`/facility/shifts`) and `ManageBids.jsx` (`/facility/shifts/:shiftId/bids`, Accept per pending bid) + "My shifts" Home link. **Decision/deviation:** accept is a `SECURITY DEFINER` Postgres RPC, **not** an Edge Function — chosen for atomic multi-row work and to avoid exposing `service_role`; same security boundary (no client `UPDATE bids`). The architecture's separate webhook-driven `auto-cancel-bids` is folded into this RPC; SMS-on-decline still deferred. **Done-checks passed:** `vite build` + lint clean; verified live (non-owner blocked; accept → other bid cancelled → shift filled). **Advisors:** only two remain, both expected — `accept_bid` is intentionally `authenticated`-callable (authorizes internally via `auth.uid()`), and "leaked password protection" is a one-click Auth dashboard toggle to enable before launch.
- [x] **Shift completion flow (check-in + dual confirmation).** `supabase/migrations/018_shift_completion.sql` — `check_in_shift(p_shift_id)` RPC (accepted professional only; `filled → in_progress`) + `complete_shift_on_dual_confirm()` trigger on `shift_confirmations` (when both timestamps set, `in_progress → completed`, fires INV-07). `019` tightens grants (anon/PUBLIC revoked; `authenticated` keeps the RPC). Hooks `src/hooks/useShiftConfirmation.js` (`useShiftConfirmation`, `useCheckInShift`, `useConfirmCompletion` — picks the column by role; the guard trigger independently enforces each side writes only its own). UI: professional `ShiftDetail` shows **Check in** (filled) then **Confirm completion** (in_progress); facility `ManageBids` shows **Confirm completion** (in_progress); both show "completed". **Assumptions (spec was loose):** A1 check-in = professional action; A2 completion = dual confirm → trigger; A3 payment release deferred to Paystack; A4 buttons status-gated in the UI. **Done-checks passed:** `vite build` + lint clean; **full lifecycle verified live** (accept→filled, check-in→in_progress, single confirm→still in_progress, dual confirm→completed). **Advisors:** 3 expected (the `accept_bid` + `check_in_shift` intentional RPCs, plus the dashboard leaked-password toggle).
- [x] **Professional "My shifts" view.** Closes a gap: a professional could bid but had no view of their own accepted/upcoming work. `useProfessionalBids()` (`src/hooks/useBids.js`) — the caller's bids across all shifts with the shift + its facility embedded, newest first, 20-row page (RLS already scopes to own bids and lets them read any shift they've bid on — migrations 007/020). `src/pages/professional/MyShifts.jsx` (`/professional/my-shifts`, `allowedRoles=['professional']`) groups **Accepted work** (sorted by start time, upcoming first) above **Other bids** (pending / not-selected / cancelled), each card linking to the shift detail (where check-in / confirm / rate already live). "My shifts" link added to the professional `Home`. **Done-checks passed:** `vite build` succeeds; `eslint` clean on changed files. **Browser-test pending:** the embedded `bids → shifts → facility_profiles` read under the professional's JWT.
- [x] **`Feature-specs/09-payments.md` — Paystack payments unit complete (collection → bank linking → disbursement → read views), all 4 steps + refund-on-cancel.** Payment work is split per workflow §3 (schema → Edge Function → frontend, each verified). **Decision (user, 2026-06): the professional bears the 10% fee** — the facility is charged the **gross pay rate** (not gross + 10%); the professional receives gross − 10% at payout. Matches architecture.md (`net = gross − commission`) and the existing PostShift/ShiftDetail previews, so no doc conflict. **Built this turn — collection at posting:** `supabase/functions/verify-payment/index.ts` (deployed, `verify_jwt` on) — verifies the charge **server-side** with Paystack's Verify API (client callback never trusted), confirms the charged amount **equals the gross pay rate** (binds amount→pay rate), is **idempotent** by `paystack_reference`, then creates the shift via **service_role** (so a shift can't exist without a verified payment); 10s fetch timeout + CORS + structured JSON per code-standards. Frontend: `src/lib/paystack.js` (inline popup, public key), Paystack inline script in `index.html`, `src/hooks/usePayments.js` (`usePostPaidShift` → invokes verify-payment), `PostShift` reworked to **validate → payment-summary `ConfirmModal` → Paystack popup → server-verify → shift created**; the old client-side `useCreateShift` was **removed** (dead code). `VITE_PAYSTACK_PUBLIC_KEY` added to `.env.example`. **INV-01/INV-05 honoured:** the secret key + transfer/verify logic live only in the Edge Function, never in `src/` (grep clean). **Done-checks:** `vite build` + `eslint` clean; function deployed ACTIVE. **⚠ Needs the user before it works/can be verified:** (1) put `VITE_PAYSTACK_PUBLIC_KEY=pk_test_…` in local `.env`; (2) set `PAYSTACK_SECRET_KEY=sk_test_…` as a Supabase Edge Function secret (dashboard or `supabase secrets set`); (3) test posting with a Paystack test card. **Step 1 VERIFIED live (2026-06-03):** a facility posted a ₦50,000 shift with a Paystack **test** card → `verify-payment` returned `200` (Edge logs) → shift created `open` with a `paystack_reference` and the charged amount matched the pay rate. **Hardened:** `supabase/migrations/023_gate_shift_insert_to_payment.sql` drops the client `shifts_insert_own` INSERT policy, so shifts can now be created **only** by the service_role verify-payment function (the raw-API bypass is closed; service_role ignores RLS). **Keys:** test public key in `.env`; `PAYSTACK_SECRET_KEY` (test) set in Supabase secrets by the user. **Refund-on-cancel — BUILT (user decision: full refund minus Paystack fees).** `supabase/functions/cancel-shift/index.ts` (deployed, `verify_jwt` on): verifies the caller owns the **open** shift, calls Paystack **Verify** to read the original `amount` + `fees`, refunds `amount − fees` via the Paystack **Refund API** (so the platform doesn't eat Paystack's gateway fee), then sets the shift `cancelled` — all server-side (secret key never in `src/`). If the refund doesn't register it does **not** cancel (leaves the shift open to retry). `useCancelShift` now invokes this function instead of a direct status update; the facility Cancel confirm copy explains the refund. **Residual (benign):** the broad `shifts_update_own` RLS policy still lets a facility set their own shift `cancelled` via the raw API without the refund — but that only forfeits their own refund, so there's no incentive to bypass (unlike the insert bypass, which was closed). **Still out of scope:** cancelling a *filled* shift (needs professional-side handling) and dispute/no-show refunds. **Done-checks:** `vite build` + `eslint` clean; INV-05 clean; function deployed ACTIVE (user has the secret set, so it runs). **Step 2 (bank linking) — BUILT + deployed.** `supabase/functions/paystack-proxy/index.ts` (deployed, `verify_jwt` on) — read-only proxy for Paystack's bank list (`action:'banks'`) and account resolve (`action:'resolve'`), secret key server-side. Hooks in `usePayments.js`: `useSupportedBanks`, `useResolveBankAccount`, `useLinkBankAccount` (writes `bank_account_number`/`bank_code` on the owner's `professional_profiles` — allowed by RLS; guard only protects is_verified/avg_rating), `useOwnBankAccount` (reports linked status with only the **last 4** kept client-side — full number is sensitive, never stored client-side per code-standards). UI: `src/pages/professional/Earnings.jsx` (`/professional/earnings`) — a Bank-account card with select-bank → enter number → **Verify** (resolve name) → **Save** (enabled only after a successful verify; re-verify required if inputs change), plus a payout-history placeholder for Step 4. "Earnings" link added to professional `Home`. **Note:** the account *name* is shown for confirmation at link time but **not persisted** (no `bank_account_name` column live; transfers only need number + code). `vite build` + `eslint` clean; INV-05 clean. **Step 3 (release-payment / disbursement) — BUILT + deployed.** `supabase/migrations/024_transactions_one_per_shift.sql` adds `unique(shift_id)` on `transactions` (the concurrency lock). `supabase/functions/release-payment/index.ts` (deployed, `verify_jwt` on): invoked by the confirming client but **authoritative** — re-reads `shift_confirmations` (both timestamps) + shift `completed`, authorises the caller as a party (facility owner or accepted professional), then pays **at most once** per shift. Flow: idempotency check (existing `released`→done, `escrow`→processing) → professional bank check (else `needsBank`) → compute `net = gross − 10%` → **insert escrow row (the lock)** → Paystack create-recipient → initiate-transfer → **update row → `released`** with `paystack_transfer_code`. Failure paths mark the row `failed` (retryable); `transfer.status==='otp'` returns `needsOtp` and does NOT record as paid. INV-01 (transfer logic only in the function, not `src/`) and INV-04 (only escrow/failed rows updated, never a released one) honoured. Client wiring: `useReleasePayment` in `usePayments.js`, invoked after a completion confirm in both `ShiftDetail` (professional) and `ManageBids` (facility) — single-side confirm is a no-op (`ready:false`); the second confirm fires the payout; UI shows a payout note. `vite build` + `eslint` clean; INV-05 clean. **⚠ Cannot be verified from here — needs the user's Paystack account set up for payouts:** (a) **Transfers enabled/activated** on the account; (b) **transfer OTP disabled** (Settings → Transfers) so payouts are automatic — otherwise the function returns `needsOtp` and the transfer waits; (c) a funded (test) balance; (d) the professional must have linked a bank (Step 2). Verify via the Paystack Transfers log after a full accept→check-in→dual-confirm flow. **Step 4 (read views + payout retry) — BUILT.** `useProfessionalEarnings` + `useFacilityTransactions` in `usePayments.js`. Professional `Earnings.jsx` now shows summary cards (**Total earned** = sum released net, **Pending** = net of accepted bids on filled/in-progress shifts, **Completed** = released count) + a **payout-history** list (role · facility · date · net), above the bank-account card. New `facility/Transactions.jsx` (`/facility/transactions`, "Transactions" Home link) — spend summary (Total spent / Completed / Avg) + a transaction list (role, gross, fee, net, status, date). **Payout retry:** professional `ShiftDetail` shows a "Check payout status" button on completed shifts that re-invokes `release-payment` (idempotent), covering the stuck `failed`/`needsBank` case. `vite build` + `eslint` clean; INV-05 clean. **Verified end-to-end (simulate mode):** a ₦33,000 shift produced a `released` transaction (net ₦29,700, fee ₦3,300). **Payments unit (spec 09) complete** for the agreed scope. **Deferred / launch-prep:** SMS-on-release (Termii); a webhook/cron backstop so a dropped confirming client never misses a payout; **remove/guard `PAYMENTS_SIMULATE`** before production; facility Transactions omits professional name (deep embed); Transfers require Paystack business activation for real (non-simulated) payouts.
- [~] **`Feature-specs/08-bidding.md` reconciled — bidding core already built; remaining gaps added.** The whole bid lifecycle already existed: `bids` table + INV-02 (verified) / INV-03 (overlap) triggers, `useBids` (submit / my-bids / shift-bids / accept), `BidButton`, `ManageBids`, and the `accept_bid` SECURITY DEFINER RPC that **atomically accepts one bid, cancels the competing pending bids, and sets the shift `filled`** — the documented replacement for the spec's `auto-cancel-bids` Edge Function + DB webhook. `/professional/my-shifts` already serves as the professional bid history (grouped Accepted / Other). **Built this session:** (1) **Confirmation modal before accepting** in `ManageBids` ("Accepting … will decline all other bids") via the shared `ConfirmModal` — previously accept was immediate. (2) **Bid count** ("N professionals have bid on this shift") on `ManageBids`. (3) **"Add to calendar" (.ics)** link on accepted shifts in `MyShifts` — new pure util `src/utils/calendar.js` (`buildShiftIcs`/`shiftIcsHref`) rendered as a `data:` URL on an `<a download>` (no DOM manipulation, util stays pure). **Done-checks passed:** `vite build` + `eslint` clean on changed files; INV-05 re-verified. **Deferred / not duplicated:** `auto-cancel-bids` Edge Function (cancellation already done atomically by `accept_bid`); SMS to declined + accepted professionals and `notifications` rows (need the `send-sms` Edge Function + Termii); a separate tabbed `/professional/bids` page (would duplicate the existing `/professional/my-shifts` bid history — the 4-tab layout is a deferred UX variant); "View Profile in a slide-over panel" (current links to the full profile page; slide-over is visual, UI deferred). **Not followed:** spec migration `007` (already applied; spec's per-insert/per-update subquery-RLS form superseded by the live triggers + `private` SECURITY DEFINER helpers).
- [~] **`Feature-specs/07-shift-marketplace.md` — non-payment gaps implemented; Paystack payment blocked + commission-model conflict surfaced.** The feed, detail, post form, facility list, `ShiftCard`, and bidding already existed (units B1/B2 + posting). **Built this session (all non-payment, architecture-consistent):** (1) `src/constants/fees.js` (`COMMISSION_RATE = 0.1`, kept out of components/hooks per code-standards) + `calculateCommission`/`calculateNet` in `money.js`. (2) `useOpenShifts` gained a **Min Pay filter** (`minPayKobo` → `gte pay_rate_naira`) and **Load More pagination** (`hasMore`/`loadMore`, page cursor); `ShiftFeed` got the Min-pay input, a Clear-filters link, and a Load-more button. (3) `useCancelShift` (open → cancelled; INV-07 + facility RLS) + a confirm-gated **Cancel** action on the facility shift list. (4) `ShiftDetail` now shows the professional **pay breakdown** ("You earn after 10% platform fee" = `calculateNet`). (5) `PostShift` shows a **live fee preview** (platform fee + professional net) as the pay rate is typed. **Done-checks passed:** `vite build` + `eslint` clean on all changed files; INV-05 re-verified. **BLOCKED — Paystack upfront payment (the spec's headline goal):** not built. It needs (a) a Paystack **public key** in `.env` (`VITE_PAYSTACK_PUBLIC_KEY`) + the inline script, (b) a **server-side verify Edge Function** using the secret key (INV-01 — secret never on the frontend), and (c) the frontend popup/`PaymentSummaryModal` trigger — and per workflow §3 payment work must be split into those three independently-verified steps. So `PostShift` still creates the shift directly as `open` with `paystack_reference = null`. **Commission model — RESOLVED: 10% (architecture structure).** The structure question (spec 07's `calculateFacilityTotal = gross + commission` vs architecture's `net = gross − commission`) was settled in favour of **architecture**: facility pays the posted gross; platform keeps the commission; professional receives gross − commission. `calculateFacilityTotal` (the gross+fee model) was **not** added. On the rate: the user briefly chose 5% then said "leave it at 10% for now" — so `COMMISSION_RATE = 0.1` in `src/constants/fees.js` (matches `architecture.md` / `code-standards.md`, no doc discrepancy). `calculateNet`/`calculateCommission` and all UI percent copy derive from that single constant, so a future rate change is a one-line edit. The (not-yet-built) `release-payment` Edge Function must use the same 10%. **Deviations not followed:** spec migration `006` (already applied as live `006`; live uses free-text `role_required` with **no `role_label`** column and `pay_rate_naira` (kobo) rather than the spec's enum+`role_label`+`pay_rate_kobo` — restructuring working schema/code not done); redundant WAT date utils + `StatusBadge`/skeleton cards (visual, UI deferred); `getBidCountForShift` (RLS blocks a professional from reading others' bids — would need a SECURITY DEFINER count RPC; deferred).
- [~] **`Feature-specs/06-admin-panel.md` — clean subset implemented; rest pending two decisions.** Admin RLS already complete via `is_admin()` (JWT) on every table — incl. `transactions_select_parties` (admin reads all) — so the spec's migration `005` (redundant + uses the recursion-prone `select from users` subquery form) was **not** added. The credential queue (`useAdminCredentials` + `CredentialQueue`) already existed. **Built this session:** (1) `src/components/ui/ConfirmModal.jsx` — reusable confirm dialog on the shadcn Dialog primitive (dumb; parent owns open state). (2) `src/hooks/useAdmin.js` — `useAdminDashboard()` (counts: pending credentials/facilities, total users, total revenue = released-transaction commission sum, 0 until payments; **note:** move the sum to a DB aggregate before real volume), `usePendingFacilities()`, `useVerifyFacility()` (`approveFacility` flips `is_verified` — the guard trigger permits it for the admin JWT). (3) `src/pages/admin/Dashboard.jsx` (`/admin/dashboard`) — 4 stat cards + queue links. (4) `src/pages/admin/FacilityQueue.jsx` (`/admin/facilities`) — unverified-facility list + Verify action gated behind `ConfirmModal`. Admin `Home` now links Dashboard / Credentials / Verify facilities; routes added (`allowedRoles=['admin']`). **Done-checks passed:** `vite build` succeeds; `eslint` clean on changed files; INV-05 re-verified. **Deferred (blocked):** SMS on approve/reject (needs the `send-sms` Edge Function + Termii); rejection-reason capture (live `credentials` has no `rejection_reason` column — would need a migration); auto-verify-on-3-approved + two-panel document viewer (refinements to the working credential queue, which opens the signed URL in a new tab). **User decisions taken (this session):** **(1) User Manager — built WITH enforcement.** `useUsers({role,status,search,page})` + `useUpdateUserStatus` (`suspendUser`/`reactivateUser`; suspend = "ban" for MVP) in `useAdmin.js`; `src/pages/admin/UserManager.jsx` (`/admin/users`) — email search, role tabs, status filter, paginated list, Suspend/Reactivate (confirm-gated, admins not suspendable), View profile. **Enforcement:** added `status` to `authStore` (protected file — changed with explicit user approval) + `useAuth` now fetches `users.status` in `syncAuthStore`; `ProtectedRoute` redirects `status === 'suspended'` to the new `/auth/suspended` screen (`src/pages/auth/Suspended.jsx`, left outside `ProtectedRoute` to avoid a redirect loop). **Only `suspended` is blocked** — `pending`/`active` pass through, so the live immediate-access model and onboarding are unaffected (consistent with the 03-auth decision). **Limitation (documented, not a bug):** this is a **client-side route gate**, not server-enforced — `users.status` is not checked in any RLS policy, and a user suspended mid-session keeps access until their next session sync (reload/token refresh). Hardening (status in RLS or session revocation) is a future task. **(2) Transactions log — deferred** to the payments unit (table is empty until payments ship; professional-name derivation via the accepted bid is better exercised then). **Done-checks passed:** `vite build` + `eslint` clean on all changed files; INV-05 re-verified. Remaining 06 items still deferred: SMS on approve/reject, rejection-reason capture (needs a column), auto-verify-on-3 + two-panel viewer.
- [x] **`Feature-specs/05-facility-profile.md` reconciled + gaps implemented (incl. a schema migration).** Facility analog of spec 04; the `facility_profiles` table (migration 004), `useSaveFacilityProfile`, facility onboarding, the public facility view, and the shared `VerifiedBadge` (now with a pending state) already existed. **Schema change (user-approved):** `supabase/migrations/022_add_facility_contact_phone.sql` — adds **`facility_profiles.contact_phone`** (text, **nullable** so the 2 existing rows aren't broken), applied live via MCP and verified by `list_tables`. The spec requires a facility contact phone and the live table had none. **Gaps built:** (1) `validateCACNumber` (`(RC|BN)\d{5,10}`, case-insensitive) + `validateNigerianPhone` added to `src/utils/validators.js`. (2) `facility/Onboarding.jsx` — added the contact-phone field, CAC-format validation, and phone validation; `useSaveFacilityProfile` now writes `contact_phone`. (3) `useOwnFacilityProfile(userId)` (`src/hooks/useProfile.js`) — an **owner-scoped** read that includes `cac_number`/`contact_phone` (kept out of the public `FACILITY_PROFILE_SELECT` since `cac_number` is sensitive). (4) **`src/pages/facility/MyProfile.jsx` at `/facility/profile`** — the missing editable facility owner page: view (name, type, CAC, address, location, contact name/phone, verified-or-pending badge, avg rating shown only once rated) + inline edit (**CAC locked after creation**, phone re-validated, reuses `useSaveFacilityProfile`). Home's facility "My profile" link repointed here (cross-role public view stays at `/facilities/:userId`). **Spec items deliberately NOT followed:** spec migration `004` (already applied); `FacilityCard` component (no consumer — professionals don't see bid lists, and `ShiftCard`/`ShiftDetail` already render facility name; building it now = dead code); `/facility/profile/setup` route naming + status-`pending` redirect (superseded by prior decisions); "segmented selector" facility-type / single-option Abuja dropdown (visual polish deferred — uses the existing FCT select). **Privacy note:** `contact_phone` is currently shown only on the owner page, not the public facility view (no explicit spec requirement to expose it). **Scope note:** Register still has its own inline Nigerian-phone regex; could later consolidate onto `validateNigerianPhone` (left untouched per "don't touch unrelated files"). **Done-checks passed:** migration applied + column verified live; `vite build` succeeds; `eslint` clean on all changed files; INV-05 re-verified. **Browser-test pending:** the facility edit upsert (incl. `contact_phone`) and `useOwnFacilityProfile` read under the facility's JWT.
- [x] **`Feature-specs/04-professional-profile.md` reconciled + gaps implemented.** The spec ("Spec 02 — Professional Profile & Credential Upload") predated the build; the `professional_profiles`/`credentials` tables (migrations 003/005), private `credentials` bucket (012), two-step onboarding (`/professional/onboarding` → `/professional/documents`), upload hook, `VerifiedBadge`, and the read-only profile view already existed. **Genuine gaps built:** (1) `src/utils/validators.js` — `validateCouncilRegNumber(specialty, number)` (per-specialty formats MDC/PCN/NM/MLS straight from the spec) + `COUNCIL_REG_HINTS` + `validateFileUpload(file)` (bucket-accurate). (2) Council-reg format validation wired into `professional/Onboarding.jsx`; `professional/DocumentUpload.jsx` switched from its inline check to `validateFileUpload` (no dead code). (3) `VerifiedBadge` gained a **Pending review** state (`pending` prop; backward-compatible). (4) `useOwnCredentials()` (`src/hooks/useCredentials.js`) — the professional's own credential statuses (RLS `credentials_select_own`). (5) **`src/pages/professional/MyProfile.jsx` at `/professional/profile`** — the missing editable own-profile page: view (name, specialty, council reg, experience, preferred-city tags, bio, verified/pending badge, avg rating shown only once rated) + inline edit (specialty locked after creation, council-reg re-validated, reuses `useSaveProfessionalProfile`) + a credential-status section. Home's professional "My profile" link repointed here (the cross-role public view stays at `/professionals/:userId`, reached from bid/shift links). **Two correctness fixes during the work:** live `credentials` has **no `rejection_reason`** column (spec's does) — dropped from the select; added `council_reg_number` to `PROFESSIONAL_PROFILE_SELECT` (the owner page needs it; RLS already exposes it). **Spec items deliberately NOT followed:** file validation 5 MB + WEBP (conflicts with the live bucket's 10 MB + PDF/PNG/JPEG — migration 012 is source of truth); bank-account section / `bank_account_name` / `linkBankAccount` (deferred to the payments unit; no such column); `/professional/profile/setup` route naming, status-`pending` redirect, "Abuja/Lagos" cities (superseded by the live-model / FCT-only / existing-route decisions); components under `components/profiles/` (live `VerifiedBadge` stays in `components/profile/`, not duplicated). **Scope note:** this spec is professional-only — no facility edit page built. **Done-checks passed:** `vite build` succeeds; `eslint` clean on all changed files; INV-05 (`service_role` absent from `src/`) re-verified. **Browser-test pending:** the edit upsert + `useOwnCredentials` read under the professional's JWT.
- [x] **Profile view pages (professional + facility).** Public profile display — the remaining half of ratings (submission + avg maintenance were done previously). **Read hooks** (`src/hooks/useProfile.js`): `useProfessionalProfile(userId)` / `useFacilityProfile(userId)` — named-column reads (no `SELECT *`; `bank_account_number`/`bank_code`/`cac_number` deliberately excluded as sensitive), return `{ profile, loading, error, refetch }`. `useUserRatings(userId)` added to `src/hooks/useRatings.js` — a user's received ratings, newest-first, 20-row page. `formatDate()` added to `src/utils/dateTime.js`. **Dumb components** (`src/components/profile/`): `RatingStars.jsx` (read-only 1–5 star + numeric avg, "Not yet rated" when null), `VerifiedBadge.jsx` (renders nothing when unverified), `RatingList.jsx` (history list; reuses `RatingStars`). **Pages:** `src/pages/professional/ProfessionalProfile.jsx` (`/professionals/:userId`) and `src/pages/facility/FacilityProfile.jsx` (`/facilities/:userId`) — thin, compose the hooks + components. **Routes are cross-role** (gated on auth only, not `allowedRoles`): a facility views a professional and vice versa, and RLS already allows any authenticated user to read any profile (migrations 003/004 `*_select_all`). **Wiring:** "View my profile" link added to `Home` (per role); the bidder name in `ManageBids` links to `/professionals/{id}`; the facility name in `ShiftDetail` links to `/facilities/{id}`. **Done-checks passed:** `vite build` succeeds (1937 modules); `eslint` clean on all changed files (the 9 remaining lint errors are all in the untouched shadcn `src/components/ui/*` primitives). **Browser-test pending:** the cross-role profile reads + ratings list render under the viewer's JWT (RLS select-all paths). **Note:** rating history shows score/comment/date without rater attribution — the `ratings.rater_user_id` → `users` FK doesn't reach a display name in one embed (names live in the role profile tables), so rater names are deferred rather than risk an ambiguous nested embed.
- [x] **Ratings (submission + avg maintenance).** `supabase/migrations/021_ratings_avg.sql` — `update_ratee_avg_rating()` trigger recomputes the ratee's `avg_rating` on each insert and writes it to their profile; the profile guards were updated to allow that system write via a transaction-local `app.is_system_update` flag (so `avg_rating` is system-maintained while `is_verified` stays admin-only). Frontend: `src/hooks/useRatings.js` (`useShiftRating`, `useSubmitRating`) + `src/components/ratings/RatingForm.jsx` (dumb 1–5 star + comment) wired into `ShiftDetail` (professional rates the facility) and `ManageBids` (facility rates the accepted professional), shown only when the shift is `completed`; the DB enforces completed-only, party-only, one-per-direction (migration 010). `facility_id` added to `SHIFT_SELECT` so the professional knows the ratee. **Done-checks passed:** `vite build` + lint clean; **verified live as the authenticated parties** — both directions insert under RLS and `avg_rating` updates (5.0 / 4.0). **Deferred:** public display of `avg_rating` on profile *pages* (no profile view pages exist yet; the value is maintained and ready). Advisors unchanged (3 expected).

- [x] **Design phase — promote Premium Green to primary (step 16).** User: "the black is overpowering, make it premium green especially the buttons. do it intuitively." Step 15 had green as a subtle accent; this makes **Premium Green the primary brand colour** while Matte Black recedes to text. **Done at the token level** (every screen uses semantic tokens, so one remap propagates): `index.css` `:root` — `--primary`/`--ring`/`--sidebar-primary`/`--sidebar-ring`/`--chart-1` → `var(--brand-green)`. The shadcn **default Button** variant changed from `bg-foreground text-background` (matte black) → `bg-primary text-primary-foreground hover:bg-[var(--brand-green-hover)]` — so all primary buttons are now green. This automatically greened everything on `bg-primary`/`text-primary`/active-state: every primary button, sidebar active nav, stat-card icon chips, the top-bar user chip, the `DashboardHero` banner, and the landing CTA card / product-preview welcome banner / pill-nav "Get started". Matte Black stays as `--foreground` (headings + body). **Logo visibility fix:** the green logo bars would vanish on the near-black `AuthLayout` panel, so the mark fill is now a `--logo-mark` token (`#0A3D2D` light → `#34D399` on `.dark`); `Logo.jsx` rects use `fill="var(--logo-mark)"`. **Done-checks passed:** `vite build` succeeds. Context docs updated (`ui-context.md` UPDATE note + token table now describe green-primary; this line). **Follow-up (same step):** user swapped the deep `#0A3D2D` for a **vibrant mid-tone green `#3ECF8E`** (Supabase green, HSL 154/71/53 — the deep one read as black). Because `#3ECF8E` is light, **`--primary-foreground` flipped #FFFFFF → `#1A1A1A`** (dark text on green, the Supabase fill pattern); `--brand-green-hover #2FB878`, `--brand-green-light #E7FAF1`; `--logo-mark` now the brand green on both surfaces (dropped the `#34D399` dark-brighten). Fixed white-on-green spots that would've gone invisible: `DashboardHero` badge `bg-white/15 → bg-foreground/10`; both dashboards' hero CTA + the landing CTA-card button + landing product-preview chip `bg-white text-primary → bg-foreground text-background` (black button on green reads better than low-contrast green-on-white); landing preview eyebrow `text-white/70 → text-primary-foreground/70`. `vite build` clean.
- [x] **Design phase — official tri-colour brand palette (step 15).** User supplied the official brand colours — **Premium White `#FFFFFF`**, **Matte Black `#1A1A1A`**, **Premium Green `#0A3D2D`** — with green to be used *subtly* ("I like the landing page but you can add the Premium Green subtly"). This supersedes step 14's pure-monochrome: the disliked "green" earlier was the retired teal; this deep forest green is the real signature. **Changes:** (1) `index.css` — `--brand-primary #111111 → #1A1A1A` (Matte Black); added `--brand-green #0A3D2D` (+ `-hover #0D4D39`, `-light #ECF3F0`) and exposed `--color-brand-green*` in `@theme`; all `:root` text/surface `#111111` → `#1A1A1A` for matte-black cohesion. (2) **Green given an intentional home rather than scattered** — the **logo mark** (`Logo.jsx` + `favicon.svg` bars) is now Premium Green (wordmark stays matte black). (3) **Landing subtle green accents:** hero eyebrow pill (green text + dot + faint green border/tint), the one highlighted hero word "paid" (`text-brand-green`), how-it-works step markers (`bg-brand-green/10 text-brand-green`), and one social-proof avatar gradient. Everything else stays matte-black/neutral/amber. Context docs updated (`ui-context.md` brand-token table + UPDATE note; this line). **Done-checks passed:** `vite build` succeeds; no `#111111`/`0B6E6E` refs remain in `src`.
- [x] **Design phase — teal→black rebrand + landing rebuild (step 14).** **(a) Landing rebuilt** on the single-light-surface template studied from datafa.st / Podia / June / Uvodo: one `bg-secondary` surface throughout (no dark/light bands), floating pill nav with center anchor links, centered hero (eyebrow pill → big bold headline → subtitle → brand CTA → social-proof avatar row), a **browser-framed product preview** of the real app (welcome banner + stat cards + open-shifts list), value columns, how-it-works, a contained CTA card, light footer. `Reveal` now forwards arbitrary props (so anchor `id`s work). **(b) Teal RETIRED → BLACK everywhere** (user: "I don't like the green"): `index.css` `--brand-primary #0B6E6E → #111111` (+ hover/light/muted neutralised), `--ring`/charts/sidebar-ring de-tealed, **`--muted-foreground #6B7280 → #4B5563`** so text reads less faint; `Logo.jsx` + `favicon.svg` bars teal→black; `AppLayout` bg-dots teal→neutral; `AuthLayout` glow teal→white + its value icons `text-brand-primary → text-foreground` (were going invisible on the dark panel); Landing avatar gradients de-tealed. System is now **black primary + neutral greys + amber (money) accent + semantic status badges**. Context docs updated (`ui-context.md` brand tokens + this color line). **Kept:** semantic shift-status badges (open=green etc.) — flagged to user in case they also want those monochromed. **Done-checks passed:** `eslint` clean; `vite build` succeeds; no `0B6E6E`/teal refs remain in `src`. Reference: Uvodo admin (clean top bar w/ user chip, bold page titles + right-aligned actions, small-caps section labels). **(1) Top bar user chip:** the lone notification bell now sits next to an avatar chip (teal initials circle + name-from-email + role), bordered pill on sm+. **(2) `PageHeader` component** (`components/layout/PageHeader.jsx`) — bold `text-2xl` title + subtitle + optional right-aligned `actions`; replaced the small `text-xl font-medium` ad-hoc headers on ShiftFeed, Earnings, Transactions, and both MyShifts (their header action buttons promoted from outline → primary). **(3) `SectionLabel` component** (`components/ui/SectionLabel.jsx`) — small uppercase tracked grey label; applied to the dashboards' section headings (Next shift / Recent bids / Needs your attention / Recent shifts). Fixed a duplicate `PageContainer` import in Transactions introduced during the edit. **Done-checks passed:** `eslint` clean (only the known shadcn-primitive warnings); `vite build` succeeds. **Follow-ups:** same `PageHeader` on admin queues + profiles; a two-column form for PostShift (the Uvodo "Create product" main+sidebar pattern); commit the uncommitted redesign (steps 11–13 are on top of `d090f2c`). **(a) Admin login:** the seeded admin (`abdulhafizakinsola@gmail.com`) couldn't sign in — DB was all correct (role `admin`, status `active`, email confirmed, `app_metadata.role=admin`), so it was just the never-recorded temp password. Reset via SQL (`update auth.users set encrypted_password = extensions.crypt('Locumii@Admin1', extensions.gen_salt('bf'))`); user to change it. **(b) Compact pass** (replicate the density of the reference dashboards): `PageContainer` padding `py-8/10 → py-5/6` and default section gap `gap-6 → gap-4`; `DashboardHero` shorter (`py-7/8 → py-5/6`, title `text-2xl/3xl → text-xl/2xl`); fixed stat-card **double vertical padding** (Card already has `py-4`, so `CardContent` dropped its `p-4` → `px-4`) in the shared StatCard + both dashboards' recent-cards; sidebar logo row `h-16 → h-14` + nav items `py-2 → py-1.5`; topbar `h-16 → h-14`; admin stat grid `grid-cols-2 → sm:grid-cols-4`. **Done-checks passed:** `eslint` clean; `vite build` succeeds.
- [x] **Design phase — sidebar dashboard redesign (step 11).** User asked to match the modern admin-dashboard references (Scholaro/Origin/Clay — left sidebar + colored welcome hero + icon stat cards). **(1) Left sidebar layout:** new `src/components/layout/Sidebar.jsx` (fixed `w-64`, logo, role badge, nav links with tinted-teal active state, sign-out pinned bottom; `hidden md:flex`). `AppLayout` rewritten: sidebar + a slim sticky top bar (mobile logo + notification bell) + `md:pl-64` main column; mobile keeps `BottomNav`. Old top `AppNav.jsx` deleted (unused). `PageContainer` lost its `min-h-screen` (sidebar layout owns height/bg, avoids topbar overflow). **(2) Welcome hero banner:** new `src/components/dashboard/DashboardHero.jsx` — teal `bg-primary` rounded-2xl banner with role badge + title + subtitle + optional white CTA. Wired into all 3 dashboards (pro: "Find shifts"; facility: "Post a shift"; admin: no action). Removed the inline VerifiedBadge from the dashboard headers. **(3) Landing headline reworded** (user found the old one confusing): now "Healthcare shifts, staffed and paid in **one place**." **Done-checks passed:** `eslint` clean on changed files; `vite build` succeeds. **NOT yet committed** (committed checkpoint is `d090f2c`, pre-redesign). **Remaining / possible follow-ups:** deeper per-page restyle (shift/earnings list rows, section labels in small-caps à la Origin), a mobile slide-in sidebar drawer, and committing this redesign.
- [x] **Design phase — app interactivity + landing product snapshot (step 10).** Two pieces of user feedback: post-login pages looked "bland on white," and the landing wanted a Clerk-style product preview. **(1) Landing product snapshot:** new "A peek inside" dark section (`Landing.jsx`) — a bento of real-looking mockups built from the actual design tokens: a 3-row **shift feed** (role + `StatusBadge` + ₦), a **Total earned** card (amber mono ₦) with a payout row, and a **verified professional** card (initials avatar, green `BadgeCheck`, amber star rating, an accepted-bid notification). Grid texture + teal glow, `Reveal` on scroll. Shows the core lifecycle (browse → earn → verified profile) like Clerk's component showcase. **(2) Less-bland app pages:** (a) `AppLayout` background is now a faint off-white (`bg-muted/40`) with a subtle teal dot-grid texture (24px) so white cards lift off the page instead of white-on-white; (b) the dashboard `StatCard` gained an **icon chip** (tinted teal, or amber when `accent`) + a hover shadow — wired into both dashboards (Active bids→Gavel, Upcoming→CalendarClock, Total earned→Wallet; Open shifts→Briefcase, Filled→CheckCircle2, Total spent→Wallet); (c) `ShiftCard` now has a real hover lift (`-translate-y-0.5` + shadow + border highlight) so the feed feels interactive. **Done-checks passed:** `eslint` clean on all changed files; `vite build` succeeds.
- [x] **Design phase — auth cohesion + motion (step 9).** Addressed two pieces of user feedback: the auth/app pages felt "bland vs the landing," and the site wanted Clerk/ShiftKey-style animation. **(1) Branded auth split-screen:** `src/components/layout/AuthLayout.jsx` — a two-column layout (dark brand panel with logo + 3 value points + grid texture + teal glow on the left, hidden < lg; the form on the right with a mobile-only logo). Refactored **all four** auth pages (`Login`, `Register`, `ForgotPassword`, `Reset`) onto it — dropped the bare floating `Card`, kept all validation/logic untouched; titles/descriptions/footers now flow through `AuthLayout` props. Auth pages now share the landing's aesthetic. **(2) Motion:** `src/components/layout/Reveal.jsx` — an IntersectionObserver scroll-reveal (fade + slide-up, staggered via `delay`, `motion-reduce` safe, SSR-safe initial state). Landing now animates: hero text/visual on mount (`animate-in fade-in slide-in-from-bottom`, staggered), and the stats strip, both value columns, the how-it-works steps, and the closing CTA reveal on scroll. The auth form panel gets a mount entrance; `PageContainer` adds a soft `fade-in` so every app page eases in. Uses the already-present `tw-animate-css` utilities (verified emitted in the built CSS). **Done-checks passed:** `eslint` clean on Landing + all auth + all layout files; `vite build` succeeds.
- [x] **Design phase — "fluid Clerk/ShiftKey" elevation (step 8).** Polishing toward the spacious, full-bleed feel of Clerk + the ShiftKey competitor deck. **`PageContainer` rollout COMPLETE + made strictly uniform (user feedback: logged-in pages were different widths).** **Every** authenticated page now renders through `PageContainer` at **one single width** (`max-w-4xl`) with identical responsive gutters (16→24→32px) — no per-page size variants. First pass had introduced `size="md|lg|xl"` variants (that was the inconsistency the user flagged); `PageContainer` was then collapsed to a single fixed `max-w-4xl` (no `size` prop) and all call sites stripped to bare `<PageContainer>` via sed. Migrated: 3 dashboards, ShiftFeed (keeps its `md:grid-cols-2` card grid), both MyShifts, both MyProfile, both public Profiles, Earnings, Transactions, ShiftDetail, ManageBids, CredentialQueue, FacilityQueue, UserManager, **and** the 4 previously-centred form pages (both Onboarding, PostShift, DocumentUpload — their `flex items-center justify-center` + `max-w-lg` card replaced so they top-align in the same container; their cards are now full container width). Pages with a trailing `ConfirmModal`/modal had it folded inside the container (Dialog portal → position irrelevant). Only `auth/Suspended` keeps a centred layout (a blocked-state screen, like the auth pages). **Done-checks passed:** `eslint` clean across `src/pages/**`; `vite build` succeeds; the only remaining centred wrapper is Suspended. **Landing (`Landing.jsx`) elevated:** added a faint 44px grid texture + a soft teal radial glow behind the dark hero (depth, not flat black); bumped the hero headline to `lg:text-6xl` with tighter `-0.02em` tracking; added a full-bleed **trust/stats strip** (4 mono stats — 4 professions · FCT · 10% flat · 24h payout); gave the value cards a hover lift (`-translate-y-0.5` + border highlight); widened section gutters to `lg:px-8`. **Shared container primitive:** `src/components/layout/PageContainer.jsx` — one consistent responsive gutter set (16→24→32px) + centred max-width (`md`/`lg`/`xl`) so the horizontal whitespace reads as intentional on every screen instead of varying ad-hoc `max-w-lg/2xl/3xl`. **Applied so far:** the 3 dashboards (consistent width/gutters) and the professional `ShiftFeed`, which also became a **2-column card grid on desktop** (`md:grid-cols-2`) so the feed fills the width like ShiftKey rather than a lone narrow column. **Done-checks passed:** `eslint` clean on all 6 changed files; `vite build` succeeds. **Remaining rollout (pending):** migrate the other ~14 app pages (auth forms, onboarding, profiles, my-shifts, earnings, transactions, shift detail, manage bids, admin queues) onto `PageContainer` for full consistency — mechanical wrapper swaps, queued for the next pass.
- [x] **Design phase — marketing landing page (step 7).** Net-new public page for signed-out visitors at `/` (previously `/` redirected straight to login). **New:** `src/pages/Landing.jsx` — Clerk-accurate dark hero + nav (logo, Sign in/Get started), a sample shift-card hero visual with a soft teal glow, two value columns (**For professionals** / **For facilities**, 4 cards each), a light "How it works" 3-step band, and a dark closing-CTA + footer. **Dark sections wrap their content in `.dark`** so the existing semantic tokens render on pure-black surfaces and the primary `Button` auto-inverts to white-on-black (no per-element colour overrides). Teal accents use the new `text-brand-primary`/`-muted` utilities (added to `index.css` `@theme` + a `--brand-primary-muted: #4a9a9a` var) because the `.dark` block remaps `--primary` to white — so teal needed a stable token on dark. Copy is drawn from `project-overview.md` (verified credentials, escrow, ratings, Abuja/FCT launch, flat 10%). **Routing (`App.jsx`):** `RoleHome` → **`RootRoute`** — signed-out → `<Landing/>`, signed-in → role-dashboard redirect; moved **outside** the `AppLayout` group so the landing renders chrome-free (no app nav/bottom-nav). The `*` catch-all still routes to `/`, so unknown paths land on the marketing page when signed out. **Done-checks passed:** `eslint` clean on `Landing.jsx`/`App.jsx`; `vite build` succeeds; teal/`brand-primary-muted` token confirmed in the built CSS; no dangling `RoleHome` references. **Design phase (in-app polish + landing) complete** — remaining ui-context.md items (dark-surface badge variants, a richer hero illustration, real photography) are optional future refinements.
- [x] **Design phase — mobile bottom nav (step 6).** Responsive navigation so the app feels native on phones. **New:** `src/components/layout/BottomNav.jsx` — a `fixed` bottom tab bar (icon + label per role link) shown only below `md`, with the ui-context.md active treatment (teal icon/label + 2px teal top border; inactive muted). Added a lucide `icon` to each entry in `AppLayout`'s `LINKS_BY_ROLE` (Dashboard→LayoutDashboard, Find shifts→Search, My shifts→CalendarCheck/Briefcase, Earnings→Wallet, Post shift→Plus, Transactions→Receipt, Profile→User/Building2, Credentials→FileCheck, Facilities→Building2, Users→Users). `AppNav`'s horizontal links are now `hidden … md:flex` (the bottom nav carries them on mobile) and the top bar is `justify-between` so the logo + bell + sign-out stay edge-aligned on small screens. `AppLayout` wraps `<Outlet/>` in `pb-16 md:pb-0` so the fixed bar never covers page content. **Done-checks passed:** `eslint` clean on all layout files; `vite build` succeeds. **Note:** desktop is unchanged (top horizontal nav); only the < md breakpoint gains the bottom bar.
- [x] **Design phase — amber on earnings / payout amounts (step 5).** Applied the reserved brand amber (`#D4900A`, `text-brand-accent`) to the money the **professional receives** (ui-context.md reserves amber for "Naira amounts in earnings / payout confirmation"). Added an `accent` prop to the shared `dashboard/StatCard` and the Earnings page's inline `StatCard`/`DetailRow` (amber when set, neutral foreground otherwise — no behavioural change for existing callers). **Amber now on:** professional Dashboard "Total earned" stat; Earnings "Total earned" stat + every payout-history net amount; professional `ShiftDetail` "You earn (after fee)" net. **Deliberately left neutral:** the facility's "Total spent"/Transactions amounts (money out, not earnings) and "Pending"/forecast figures — amber is for realised earnings only, keeping it meaningful. **Done-checks passed:** `eslint` clean on all 4 changed files; `vite build` succeeds; `text-brand-accent` (#d4900a) confirmed emitted in the built CSS.
- [x] **Design phase — colour-coded shift-status badges (step 4).** Replaced the flat grey status pills with the ui-context.md colour system. **New:** semantic status tokens in `src/index.css` `@theme` (`--color-status-success`/`-bg` green, `-info`/`-bg` blue, `-warning`/`-bg` amber — each a text colour + light-surface tint) + `src/components/shifts/StatusBadge.jsx` (dumb; maps a shift status → label + colour pill: **open/completed → green, filled → blue, in_progress → amber, cancelled → grey**; always shows the text label, never colour alone, per the a11y note). **Wired into** every shift-status surface: `ShiftCard` (feed), facility `Dashboard` recent-shifts (dropped its local `SHIFT_STATUS_LABELS` map), facility & professional `MyShifts` cards, `ManageBids` header, professional `ShiftDetail` header — placed beside the role title; the old inline "· {status}" text in ManageBids / professional MyShifts was removed in favour of the badge. Bid-status pills (pending/accepted/etc.) intentionally left as-is (separate from shift status; the amber "accepted" treatment is a later item). **Done-checks passed:** `eslint` clean on all 7 changed files; `vite build` succeeds; verified the Tailwind `bg-status-*`/`text-status-*` utilities are emitted in the built CSS (Tailwind silently skips unknown classes, so this was confirmed, not assumed).
- [x] **Design phase — brand logo + black primary buttons (step 3).** (1) **Logo (Concept B "shift bars", user-supplied):** new `src/components/layout/Logo.jsx` — the two-bar shift mark (full bar = filled, 70% bar = in progress; both brand teal `#0B6E6E`, lower bar at 0.28 opacity) + the lowercase **"locumii"** wordmark (Inter/Geist 800, tight tracking, `text-foreground` so it adapts light/dark). Wired into `AppNav` replacing the plain text wordmark. (2) **Favicon:** `public/favicon.svg` replaced (was the default purple Vite mark) with the mark on a white circle — teal full bar + light-teal 70% bar — matching the rendering the user shared. (3) **Black primary buttons (user preference):** changed the shadcn `Button` `default` variant from `bg-primary`(teal) to `bg-foreground text-background hover:bg-foreground/90` — i.e. near-black `#111` on white. This keeps **teal reserved** for links / active nav / focus rings / the verified badge (`--primary` is unchanged), which is the more Clerk-accurate split and matches ui-context.md's "teal is reserved for interactive accents." Because it uses `bg-foreground`/`text-background`, it **auto-inverts to white-on-black on dark surfaces** (the spec's dark-surface primary). Single point of control — no other element uses `bg-primary` except the VerifiedBadge tint (left teal). **02-editor.md reconciled (not newly built):** that spec is **leftover template scaffolding** (a generic text-editor navbar + collapsible sidebar shell, `neutral-950` dark chrome with a `PanelLeftOpen/Close` toggle) that predates the Locumii product and does **not** fit a shift marketplace; its components (`src/components/editor/*`) were already built earlier, remain **unmounted**, and still pass the spec's done-checks (compile + lint clean). It was deliberately **not wired into the app** — it conflicts with the top-nav direction in the user's own logo concept. Flagged to the user for a keep/delete decision rather than mounting contradictory chrome. **Done-checks passed:** `vite build` succeeds (1970 modules); `eslint` clean on all new/changed files (the 2 button.jsx warnings are pre-existing shadcn-primitive lint, unchanged by the one-line variant edit).
- [x] **Design phase — shared app chrome / top nav (step 2).** Replaced the scattered per-page headers with one shared, sticky top navigation so the app reads as a single product. **New:** `src/components/layout/AppNav.jsx` (presentational — logo, role-based `NavLink`s with teal active state, the notification bell, a sign-out icon button; light nav per ui-context.md) + `src/components/layout/AppLayout.jsx` (smart — owns `useAuth`/`useNotifications`, builds the per-role link set, renders `AppNav` above `<Outlet/>`; mounts the single realtime notifications subscription, formerly in App.jsx's `NotificationChrome`). **Router restructure (`App.jsx`):** authenticated routes now nest under a `<Route element={<AppLayout/>}>` layout route; auth pages (login/register/forgot/reset/suspended) stay **outside** it so they render chrome-free. Per-route `ProtectedRoute` auth+role gating is unchanged (AppLayout only adds chrome, and only once authenticated — signed-out renders a bare `<Outlet/>` and the child redirects). Removed the old fixed top-right `NotificationChrome`. **Per-page cleanup:** stripped the now-redundant header buttons the nav replaces — Sign out / My shifts / Profile / Transactions clusters on the 3 dashboards, and "Home" back-buttons across ShiftFeed, Earnings, Transactions, CredentialQueue, both MyProfile and both MyShifts pages (contextual CTAs like "Post a shift"/"Browse shifts" kept); removed the unused `useAuth`/`useNavigate`/`Button` imports left behind. Legit "Back" buttons on detail pages were left alone. **Done-checks passed:** `eslint` clean on all touched files; `vite build` succeeds (1968 modules). **Scope note:** still the pre-polish chrome — nav is desktop-oriented (links scroll horizontally on mobile rather than a bottom-nav); a mobile bottom nav, landing page, and dark hero panels remain a later pass.
- [x] **Design phase — global brand re-skin (step 1).** Applied the Locumii teal/amber brand from `Context/ui-context.md` (v3, Clerk-accurate) as a **single global token remap** in `src/index.css` — no per-screen edits needed because every screen already uses semantic shadcn tokens (`bg-background`, `text-primary`, `text-muted-foreground`, `border-input`, etc.) and never hardcodes colors (verified: only the two **unmounted** `src/components/editor/*` files use palette colors, left untouched). **Changes:** (1) `:root` remapped to the light-surface palette — `--primary`/`--ring` = teal `#0B6E6E`, pure-neutral text/borders (`#111111`/`#6B7280`/`#E5E5E5`/`#D1D5DB`), `--destructive` = `#DC2626`, charts → teal/amber/neutral; brand vars (`--brand-primary*`, `--brand-accent*`) declared once. (2) `.dark` remapped to the spec's pure-black surfaces (`#0A0A0A`/`#111111`/`#1F1F1F`, white-on-black primary) — not toggled today but kept consistent for future dark sections. (3) `@theme` adds `--font-mono` = **JetBrains Mono** (so all `font-mono` ₦ amounts re-render in it) + `--color-brand-accent` (amber, available as `text-brand-accent` for earnings amounts) and Inter fallback on `--font-sans` (Geist stays primary). (4) `index.html` — title `temp-vite`→`Locumii`, meta description, Google-Fonts link for Inter + JetBrains Mono. **Font decision (user, 2026-06-04):** Geist + JetBrains Mono (per ui-context.md v3), not the older DM Serif/DM Sans note. **Done-checks passed:** `vite build` succeeds (1967 modules, CSS 49.9 kB). **Scope note (user choice):** this is the "global re-skin first" pass — shared app chrome (top nav replacing per-page headers), a landing page, dark hero panels, and per-screen polish (incl. distinct shift-status badge colors, amber on earnings amounts) are a **later** pass to decide after reviewing this live.

-----

## In Progress

- _(nothing in progress)_

> **Editor chrome — DELETED (2026-06-04, user decision).** `src/components/editor/*` and `Context/Feature-specs/02-editor.md` were leftover Vite-starter scaffolding (a generic text-editor navbar + collapsible sidebar) that never fit the shift marketplace and stayed unmounted. The app's chrome is the shared top nav (design-phase step 2). Removed to clear dead code.

-----

## Next Up

1. **`dev-setup.md`** — Local environment setup: Node version, Supabase CLI install, environment variables required, how to run migrations, how to start the dev server.
1. **Supabase Auth config (dashboard)** — Confirm email/password provider is enabled and SMTP (Resend) is wired. The DB schema and private `credentials` bucket are done; this is the remaining piece of the original "project initialisation" item.
1. **SMS-on-approval (deferred from credential review)** — Fire an SMS to the professional when their credentials are approved. Needs the `send-sms` Edge Function + Termii (see Open Questions). The review panel currently flips `is_verified` without notifying.
1. **Paystack payment at posting** — Its own unit (needs a Paystack account/keys). Collect the shift fee + 10% upfront via the inline popup, verify server-side in an Edge Function, then store `paystack_reference` / mark the shift payable. Currently shifts are created `open` with no payment.
1. **Edge Functions** — `auto-cancel-bids`, `send-shift-reminder`, `release-payment`, `send-sms` (Deno/TS). Several deferred items above depend on these. Needs Termii + Paystack keys.
1. **Payment release on completion** — *Check-in + dual confirmation are done (above).* Remaining: the `release-payment` step that pays out the professional (minus 10%) once a shift is `completed`. Depends on Paystack (Transfer API, server-side only — INV-01) and the professional's bank details.

-----

## Open Questions

- **App name confirmed?** “Locumii” is the working title used across all documents. Confirm before buying a domain or setting up Supabase project name.
- **Abuja-only launch geography** — All spec documents scope the MVP to FCT. Confirm this before building location filters (hardcoded city list vs. open text field).
- **Admin user creation** — ✅ Resolved. First admin is `abdulhafizakinsola@gmail.com`, seeded directly in the DB (see Completed). Future admins follow the same direct-seed approach.
- **Termii vs. alternative SMS gateway** — Termii is specified in `architecture.md`. Confirm account is created and API key is available before building any Edge Function that calls `send-sms`.
- **Professional types** — Four types are in scope: Medical Doctor, Nurse, Pharmacist, Medical Laboratory Scientist. Confirm whether NYSC-posted health workers are treated as a separate type or fall under one of these four.
- **Paystack escrow mechanism** — Paystack does not have a native escrow product. The escrow model described in the spec is implemented by collecting the full amount upfront (Paystack inline) and holding it in the platform’s Paystack balance until the `release-payment` Edge Function initiates a transfer. Confirm this approach is understood before building the payment flow.

-----

## Architecture Decisions

- **Frontend:** React (Vite) — single-page app, mobile browser first, no native app for MVP.
- **Backend:** Supabase (PostgreSQL + PostgREST + Auth + Storage + Edge Functions) — no custom Express/Node server.
- **Payments:** Paystack — inline popup for collection, Transfer API (server-side only) for disbursement. The Paystack secret key never touches the frontend.
- **SMS:** Termii — all SMS routed through a single `send-sms` Edge Function; no other function or frontend code calls Termii directly.
- **Escrow:** Implemented at the application layer — funds collected upfront via Paystack inline, held in platform Paystack balance, released via Edge Function after dual confirmation.
- **Credential verification:** Manual admin review for MVP — no OCR, no government API integration.
- **Overlap prevention:** Enforced by a PostgreSQL constraint function on the `bids` table, not by application code alone.
- **Immutable ledger:** `transactions` table rows with `status = released` are locked by a PostgreSQL trigger — no UPDATE is permitted, disputes create new rows.
- **Launch geography:** Abuja FCT only. Lagos is Phase 2.
- **Color system (updated):** **BLACK primary (`#111111`)** — the original teal `#0B6E6E` was **retired** at the user's request and replaced with black everywhere (logo, favicon, buttons, links, active states, dashboard hero, landing) for a cohesive monochrome look; amber-gold accent (`#D4900A`) kept for money/earnings; critical `#DC2626`; semantic status badges kept. Secondary text darkened (`--muted-foreground` `#6B7280 → #4B5563`) so copy reads less faint. **Fonts (resolved 2026-06-04, user):** Geist (UI/headings) + JetBrains Mono (Naira amounts) per the authoritative `ui-context.md` v3 (Clerk-accurate) — supersedes the earlier DM Serif/DM Sans note, which predated v3.

-----

## Session Notes

- **Professional "My shifts" (`/professional/my-shifts`) segregated into tabs (user request):** Accepted | Pending | Not selected (last = rejected + auto-cancelled). Replaces the two stacked sections; Accepted still sorts by start time + keeps the "Add to calendar" link. Build + lint clean.
- **Payment test finding (2026-06-03):** Step 1 collection + bank-linking verified working, but the first **release-payment transfer FAILED** — `transactions` row is `status='failed'` (no transfer_code), Paystack returned 502. Bank was correctly linked (DB confirms). Almost certainly **Paystack Transfers not enabled/approved** on the account (and/or transfer OTP on). Added `console.error` logging of Paystack's recipient/transfer error to `release-payment` (redeployed v2) so the exact reason shows in function logs on the next attempt. **Known gap:** a `failed`/`needsBank` payout has **no UI retry** (the shift can't be re-confirmed) — re-test with a fresh shift; add a payout-retry affordance in Step 4. **User action needed:** enable Transfers + disable transfer OTP in Paystack, then re-test. **Update:** the account has **no Transfers tab** → Paystack business not activated (Transfers gated behind Compliance). To unblock building Step 4 meanwhile, added a **dev-only simulate path** to `release-payment` (v3): when the Edge secret `PAYMENTS_SIMULATE=true`, it runs all checks (both confirms, completed, bank linked, computes net) but **skips the Paystack transfer** and records the transaction `released` with a `SIMULATED-…` code. Strictly off unless the secret is `true`; **must never be set in production** (add a launch-prep guard). User to set `PAYMENTS_SIMULATE=true` and re-test a fresh shift.
- **Facility "My shifts" (`/facility/shifts`) segregated by status tabs (user request).** `MyShifts.jsx` now shows **Open / Filled / In progress / Completed** tabs (one status at a time) instead of a flat list. **Cancelled shifts are hidden** from this view but **kept in the DB** — the user agreed not to hard-delete them because the row holds the payment + refund record (`paystack_reference`) and FK-cascades to bids/confirmations/transactions/ratings would destroy history (INV-04 spirit). `vite build` + lint clean.

- All five foundation documents (`project-overview.md`, `architecture.md`, `ai-workflow-rules.md`, `ui-context.md`, `locumii-landing.html`) are complete and consistent with each other. No implementation code has been written yet.
- The landing page HTML (`locumii-landing.html`) is a design proof-of-concept only — it is not part of the application source tree. It should not be moved into `src/`.
- The next session should begin with `dev-setup.md` and Supabase project initialisation before any React code is written.
- All seven architecture invariants (INV-01 through INV-07) are defined in `architecture.md` and referenced in the `ai-workflow-rules.md` verification checklist. Any agent starting work must read both files before writing code.
- The Paystack escrow model is application-layer, not a native Paystack feature — this must be clearly communicated to any developer or agent picking up the payments task.
- **Implementation has begun.** The design-system spec (`Feature-specs/01-design-systems.md`) is complete. This came before `dev-setup.md`/Supabase init (the original "Next Up" order) at the user's explicit direction. Node.js LTS (v24) was installed on the dev machine to enable the npm/npx toolchain.
- **Resolved spec conflict:** the design-system spec requested `lib/utils.ts`, but `code-standards.md` forbids TypeScript. Per user decision, the `cn()` helper stays at `src/lib/utils.js` (JavaScript). The spec's `.ts` is treated as a typo for this JS-only project. If the spec text should be corrected, that is a human edit (spec files are not auto-edited to match code).
- shadcn uses the **Nova** preset (Geist + Lucide) and **neutral** base color, aligning with `ui-context.md`. The stock shadcn theme tokens in `src/index.css` are **not yet** mapped to the Locumii teal/amber brand tokens — wiring the full brand token system is a separate, not-yet-scoped task.
- **Database schema is live (migrations 001–013, applied via Supabase MCP).** Implementation notes / minor deviations from `architecture.md`, surfaced here rather than edited into the spec (per `ai-workflow-rules.md` §5/§6 — spec files are human-edited only):
  - **Money columns hold kobo, not Naira.** Per user decision, the `*_naira` column names from `architecture.md` are kept, but the stored values are integer **kobo** (per `code-standards.md`). Column comments document this. `money.js` must divide by 100 for display. If the spec should be corrected to say `_kobo`, that is a human edit.
  - **Two RLS helper functions live in a `private` schema** (`private.is_shift_facility`, `private.is_accepted_shift_professional`), not `public`. They are `SECURITY DEFINER` (needed to avoid RLS recursion on `bids`/`shifts`); moving them out of the API-exposed `public` schema clears the advisor's "SECURITY DEFINER callable via RPC" warning. `public.is_admin()` stays in `public` (it is `SECURITY INVOKER` and only returns whether the caller is an admin).
  - **Shifts read access was extended** so a professional can still read a shift they have bid on once it leaves `open` status (`shifts_select_own_bids` policy in `007`). `architecture.md`'s RLS table only lists "open rows readable by any authenticated user" — this is a necessary corollary, not a contradiction.
  - **The `users` registration insert has no client RLS policy by design.** Inserting a `users` row is deferred to the auth-flow unit and must run through a `SECURITY DEFINER` trigger on `auth.users`, so a user cannot self-assign `role = 'admin'`.
  - **Deferred (not blocking, MVP scale):** the performance advisor's `auth_rls_initplan` warnings (wrap `auth.uid()`/`is_admin()` in `(select …)` inside policies) and one `multiple_permissive_policies` note on `shifts` SELECT remain. These are scale optimisations on currently-empty tables; revisit before launch via a dedicated policy-optimisation migration. `unused_index` INFOs are expected (no traffic yet).
- **RLS must be tested as the `authenticated` role, not via raw SQL.** `migration 020` fixed an "infinite recursion detected in policy for relation shifts": `shifts.shifts_select_own_bids` and `bids.bids_select_scoped` each had an inline `exists(... other table ...)` subquery, and each re-triggered the other table's RLS → mutual recursion. It surfaced only in the browser (a facility posting a shift, where the insert's `returning` triggers a SELECT) — **not** in any `execute_sql` test, because those run as a superuser that bypasses RLS. Fix: route both cross-table checks through `SECURITY DEFINER` helpers in `private` (`is_shift_facility`, new `has_bid_on_shift`). **Lesson:** verify RLS by `set local role authenticated; set local request.jwt.claims = '{"sub":"…"}';` (or in-browser), never just as postgres. Cross-table RLS subqueries are a recursion trap — always use a `private` SECURITY DEFINER helper.
- **`Feature-specs/03-auth.md` reconciled against the live auth flow (no rebuild).** The spec (internally "Spec 01 — Authentication & Registration") was authored before the auth unit was built and ~80% already exists, often in a more secure form. Outcome of the reconciliation:
  - **Two product decisions taken (user, this session):** (1) **Account-status model — keep the live model:** sign-up grants immediate onboarding access; admin `is_verified` gates *bidding* only. The spec's `status='pending'` → "under review/48h" gating was **dropped** (it would have locked every existing user out of onboarding, since `users.status` defaults to `'pending'` and nothing ever flips it to `'active'`). So `/auth/pending`, `/auth/suspended`, and status-based route gating are **not** implemented. (2) **Full name stays captured in onboarding** (`*_profiles.full_name`), **not** at registration — no `users.full_name` column / migration added.
  - **Spec items deliberately NOT followed (regressions/standards conflicts):** client-side `public.users` insert (live design uses the `handle_new_user` SECURITY DEFINER trigger so `role` can't be self-assigned — migration 014); a fresh `001_create_users.sql` (already applied as `002`; applied migrations are immutable); `ProtectedRoute` as a store-importing `components/ui/` file (violates "components don't import store/"; the inline-in-`App.jsx` guard is the compliant form); login → `/professional/dashboard` / `/facility/dashboard` (dashboards don't exist; no scaffolding per workflow §1). Path naming `/auth/reset-password` left as the live `/auth/reset` (changing it needs a Supabase dashboard redirect-URL change).
  - **Already satisfied (verified):** generic login error below the form (not an alert); ForgotPassword shows the same "if an account exists…" confirmation regardless of whether the email exists; auth pages gated from signed-in users via `PublicOnlyRoute`.
  - **One change made:** tightened the register phone validation from `/^\+?\d{10,14}$/` to a Nigerian-format check (`0[789]\d{9}` or `+234[789]\d{9}`) in `src/pages/auth/Register.jsx`, per the spec's "Nigerian format" requirement and code-standards "validate external input." `vite build` + lint clean; INV-05 (`service_role` absent from `src/`) re-verified.
- **Lint vs. fetch-on-mount:** the `react-hooks/set-state-in-effect` rule (eslint-plugin-react-hooks v7) errors on the architecture's prescribed "hooks own data fetching" pattern (a `useEffect` that calls a fetch which `setState`s). `usePendingCredentials` uses a one-line `// eslint-disable-next-line react-hooks/set-state-in-effect` for the mount fetch. Future list hooks (`useShifts`, `useBids`, …) will need the same. If this proliferates, consider setting that rule to `warn`/`off` in `eslint.config.js` project-wide — a deliberate config decision for the user, not done unilaterally.